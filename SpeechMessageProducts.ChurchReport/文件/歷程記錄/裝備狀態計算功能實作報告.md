# 裝備狀態計算功能實作報告

## 修改日期
2024年

## 需求說明
**原始問題**：裝備狀態並沒有正確的顯示本階段可以結業值為 TRUE 的上課紀錄單

**解決方案**：實作裝備狀態計算邏輯，從上課紀錄（StorLessons）中篩選出「本階段可以結業」（CurrentComplete = TRUE）的課程

## 已完成的修改

### 1. 創建 EquipmentStatusCalculator.cs（可選）
- **路徑**：`ChurchReport\WebServiceConnector\EquipmentStatusCalculator.cs`
- **功能**：獨立的裝備狀態計算器類別（備用方案）

### 2. 修改 DownloadIntegrateData.cs
- **路徑**：`ChurchReport\WebServiceConnector\DownloadIntegrateData.cs`
- **修改內容**：
  1. 添加 `CalculateEquipmentStatusForContact` 方法
  2. 在 `GetAllMemberDataFromPresentRecord` 方法中調用計算邏輯

## 實作邏輯

### CalculateEquipmentStatusForContact 方法
```csharp
private string CalculateEquipmentStatusForContact(Guid contactId)
{
    // 1. 取得連絡人的所有上課紀錄（StorLessons）
    var storLessonsCollection = m_ToolUtilityClass.RetrieveManyToOneRelationship(
        "contact", "contactid", contactId.ToString(), 
        "new_contact_new_stor_lessons", "new_stor_lessons");
    
    // 2. 關鍵課程階段清單
    string[] keyStages = new string[] 
    { 
        "E1", "E2", "E3", "成長班", "門徒班", "領袖班", 
        "福上", "福中", "福下" 
    };
    
    // 3. 篩選可以結業的課程
    foreach (var storLessonsEntity in storLessonsCollection.Entities)
    {
        var storLesson = m_ToolUtilityClass.RetrieveEntity("new_stor_lessons", storLessonsEntity.Id);
        
        // 檢查「本階段可以結業」欄位
        bool canGraduate = m_ToolUtilityClass.GetEntityBoolAttribute(storLesson, "new_current_complete");
        
        if (canGraduate)
        {
            // 取得階段名稱並檢查是否為關鍵階段
            var discipleLessonId = m_ToolUtilityClass.GetEntityLookupAttribute(storLesson, "new_new_disciple_lessons_new_stor_les");
            var discipleLessonEntity = m_ToolUtilityClass.RetrieveEntity("new_disciple_lessons", discipleLessonId);
            string stageName = m_ToolUtilityClass.GetEntityStringAttribute(discipleLessonEntity, "new_now_stage_name");
            
            // 如果是關鍵階段，加入結果清單
            if (!string.IsNullOrEmpty(stageName) && keyStages.Any(stage => stageName.Contains(stage)))
            {
                graduatableStages.Add(stageName);
            }
        }
    }
    
    // 4. 格式化輸出
    var distinctStages = graduatableStages.Distinct().ToList();
    if (distinctStages.Count == 0) return "";
    if (distinctStages.Count <= 3) return string.Join("、", distinctStages);
    return string.Join("、", distinctStages.Take(3)) + "等";
}
```

### 在 GetAllMemberDataFromPresentRecord 中的應用
```csharp
// 原本的程式碼（讀取 CRM 欄位）
String aEquipmentStatus = "";
if (aContactEntity.Attributes.Contains("new_equipment_status"))
{
    aEquipmentStatus = (string)aContactEntity.Attributes["new_equipment_status"];
}

// 修改後（計算實際可結業的課程）
aEquipmentStatus = CalculateEquipmentStatusForContact(aContactEntity.Id);
```

## 資料來源對應

### CRM 資料表關聯
1. **contact**（連絡人）
   - `new_equipment_status`：原本的裝備狀態欄位（不再使用）

2. **new_stor_lessons**（上課紀錄單）
   - `new_current_complete`：本階段是否可以結業（布林值）
   - `new_new_disciple_lessons_new_stor_les`：關聯到課程（Lookup）

3. **new_disciple_lessons**（課程）
   - `new_now_stage_name`：階段名稱（E1, E2, 成長班等）

## 顯示規則

### 關鍵課程階段
只顯示以下階段的可結業課程：
- E1, E2, E3
- 成長班、門徒班、領袖班
- 福上、福中、福下

### 格式化規則
- **0 個可結業課程**：顯示空字串
- **1-3 個課程**：全部顯示，用「、」分隔（例如：「E1、成長班、門徒班」）
- **4 個以上課程**：顯示前 3 個 + 「等」（例如：「E1、E2、成長班等」）

## 還需完成的工作

### ?? 未完成的修改點
以下兩個方法也需要類似的修改（目前尚未完成）：

1. **GetAllMemberDataFromList**（約第 1300-1450 行）
   - 功能：當沒有找到週報時，從名單載入成員資料
   - 需要修改：讀取 EquipmentStatus 的部分

2. **SetAllMemberDataByPersonalReport**（約第 1480-1550 行）
   - 功能：個人回報時設定成員資料
   - 需要修改：讀取 EquipmentStatus 的部分

### 修改方式
在這兩個方法中，找到以下程式碼：
```csharp
// 組員的裝備狀態
String aEquipmentStatus = "";
if (ContactEntity.Attributes.Contains("new_equipment_status"))
{
    aEquipmentStatus = (string)ContactEntity.Attributes["new_equipment_status"];
}
```

替換為：
```csharp
// 組員的裝備狀態
String aEquipmentStatus = "";
if (ContactEntity.Attributes.Contains("new_equipment_status"))
{
    aEquipmentStatus = (string)ContactEntity.Attributes["new_equipment_status"];
}

// 計算實際的裝備狀態（本階段可以結業的課程）
aEquipmentStatus = CalculateEquipmentStatusForContact(ContactEntity.Id);
```

## 測試建議

### 測試案例

#### 1. 有上課紀錄且有可結業課程
- 連絡人有多筆 StorLessons
- 其中部分 CurrentComplete = TRUE
- **預期結果**：顯示可結業的階段名稱

#### 2. 有上課紀錄但沒有可結業課程
- 連絡人有 StorLessons 但全部 CurrentComplete = FALSE
- **預期結果**：顯示空字串

#### 3. 沒有上課紀錄
- 連絡人沒有 StorLessons
- **預期結果**：顯示空字串

#### 4. 多個可結業課程
- 連絡人有 5 個可結業課程
- **預期結果**：顯示前 3 個 + 「等」

#### 5. 非關鍵階段課程
- 連絡人有可結業課程但階段不在關鍵清單中
- **預期結果**：不顯示該課程

## 性能考量

### ?? 潛在性能問題
每個成員都會執行以下查詢：
1. 查詢該成員的所有 StorLessons（1 次查詢）
2. 對每個 StorLesson 查詢完整實體（N 次查詢）
3. 對每個可結業的課程查詢 DiscipleLesson（M 次查詢）

### 建議優化方案
1. **批次載入**：一次載入所有成員的 StorLessons
2. **快取機制**：將計算結果快取到記憶體中
3. **延遲計算**：只在需要顯示時才計算
4. **資料庫欄位**：在 CRM 中添加計算欄位，定期更新

## 相關檔案

### 已修改
- ? `ChurchReport\WebServiceConnector\DownloadIntegrateData.cs`
- ? `ChurchReport\WebServiceConnector\EquipmentStatusCalculator.cs`（新增）

### 需要檢查
- ?? `ChurchReport\WebServiceConnector\DownloadEquipment.cs`（裝備模組）
- ?? `ChurchReport\Models\EquipmentDataManager.cs`（裝備資料管理）
- ?? `ChurchReport\Models\Member.cs`（Member 模型）

### 前端顯示
- ?? `ChurchReport\Views\Personal\MaintainPersonInfomationView.cshtml`
- ?? `ChurchReport\Views\Home\MaintainPersonInfomationView.cshtml`

## 注意事項

1. **資料一致性**：確保 CRM 中的 CurrentComplete 欄位正確維護
2. **錯誤處理**：當查詢失敗時返回空字串，不影響其他資料載入
3. **空值處理**：妥善處理 NULL 或空字串的情況
4. **權限控制**：前端已有裝備狀態的顯示權限控制（參考「裝備狀態顯示規則修改說明.md」）

## 後續工作

### 立即需要
1. [ ] 完成 `GetAllMemberDataFromList` 方法的修改
2. [ ] 完成 `SetAllMemberDataByPersonalReport` 方法的修改
3. [ ] 測試所有情境
4. [ ] 性能測試

### 長期優化
1. [ ] 實作批次載入機制
2. [ ] 添加快取機制
3. [ ] 考慮資料庫計算欄位
4. [ ] 添加單元測試

## 版本資訊
- **.NET Framework**：4.7.1
- **CRM 版本**：Dynamics 365
- **修改者**：AI Assistant
- **審查者**：（待填）
- **狀態**：部分完成（需完成其他兩個方法）

---
*此文件由 GitHub Copilot 自動生成*
