# 查證與報告執行計畫

- [x] 讀取相關規格、既有任務文件與附件全文，建立 Claude 主張清單。
- [x] 以 `rg` 定位相關類別、方法、測試及追蹤腳本，閱讀完整控制流。
- [x] 驗證 C3 的所有快照讀取端與資料寫入端是否共用同一同步協定。
- [x] 驗證 cache 未命中、到期、登入、切換日期與使用者資料校正流程。
- [x] 驗證背景工作的 scope、例外處理、成功／失敗紀錄與 HTTP 提示語意。
- [x] 執行不改變狀態的 Git 圖譜、測試／驗證腳本檢查；不能實測 CRM 的部分明確保留。
- [x] 依使用者最新指示完成外部模型條件調整：不以 Gemini＋Claude 雙模型作為完成門檻；本機證據已完成，Gemini 的結果僅作為可選第二意見，Claude OAuth 過期不阻斷本任務。
- [x] 撰寫 `report.md`，包含逐項裁定、發布風險矩陣及建議優先順序。
- [x] 檢查報告中的檔案／行號、命令結果與未驗證限制，確認沒有把 `bg.end` 誤寫成上傳成功。
- [x] 回覆使用者完整結論；不提交、不推送、不更動產品程式碼。

## 驗證命令

- `git status --short --branch`
- `git merge-base --is-ancestor <commit> 1.0.0.6.DesignNewArchitector`
- 專案既有相關測試命令（依方案及測試專案實際內容決定）
- `python ./.trellis/scripts/verify_trace_invariants.py <trace-directory>`（若目前資料目錄存在）

## 風險與停止點

- 外部模型結論不得直接寫入最終報告；必須回到程式碼查證。
- 若 CRM 實際操作需要使用者帳號、會改動資料或會觸發正式背景工作，只提出人工煙霧測試步驟，不代替使用者執行。
- 若發現現有未提交變更與查證範圍重疊，只讀取差異，不覆寫內容。
