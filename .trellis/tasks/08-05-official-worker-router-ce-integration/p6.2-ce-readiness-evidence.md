# P6.2 CE 唯讀驗證：Lenovo Legion 就緒與啟動閘門證據

日期：2026-08-06

## 結論

**No-Go（離線部署材料已就緒，但 Worker 尚未通過 runtime READY gate）**。

G0 與 P6.2 的本機 profile／artifact／identity chain 已通過；兩個版本的離線
`runtime.health.whoami` 與 `runtime.pool.validate.connection` harness 也已通過
`ValidateOnly`。嘗試啟動 published Gateway／Official Worker composition 時，Worker
在建立 client 後未發布 nonce-bound READY frame，Gateway 因 fail-closed startup gate
結束；因此尚未執行任何 CE operation。

## 已通過的安全與離線證據

- Windows identity 與兩個 Credential Manager target 的 readiness probe 回報
  `go`：`crm82`／`OfficialCrm82Worker` 與 `crm91`／`OfficialCrm91Worker` 均為
  `credentialTargetState: present`。
- `New-DynamicsOfficialWorkerDeployment.ps1` 回報 `outcome: provisioned`，overlay
  與兩份 `worker-profile.xml` 均由部署工具產生，且 `featureGateMustRemainDisabled` 為
  `true`。
- 兩個 alias 的離線 identity chain 均回報 `outcome: validated`、
  `operationId: runtime.health.whoami` 及 `requestExecuted: false`。
- 兩個 alias 的離線 connection-evidence chain 均回報 `outcome: validated`、
  `operationId: runtime.pool.validate.connection` 及 `requestExecuted: false`。
- deployment/readiness、compatibility 與 P6 evidence PowerShell tests、P6 focused／
  full Dynamics tests、ChurchReport MemberInfo tests，以及 Release solution build 均通過。

## 受控啟動嘗試

- 只使用 published Gateway、兩個已核准 profile、目前 Windows identity 與兩個
  allowlisted read-only operation；ChurchReport feature flag 與流量未變更。
- 已在寫入兩個核准 canonical HTTPS root URI 後重跑 startup bridge；結果仍為
  `outcome: no-go`、`reason: gateway-startup-failed-before-ready`、
  `listenerObserved: false`、`ceContacted: false`、`operationExecuted: false`，且 bridge
  已確認 `listenerReleased: true`。因此 canonical URI 已不再是目前阻塞項。
- 隔離的 named-pipe READY handshake 顯示兩個 Worker 都能連上 pipe，但都在 READY 前以
  `workerExitCode: 10` 結束；此 exit code 對應 `ClientNotReady`。這證實 Gateway 已可
  啟動 Worker process，阻塞點位於 Worker client 的外部登入／驗證就緒，而不是 manifest、
  pipe 或本機 listener 建立。
- Gateway 在本機啟動期等待 Worker READY frame；實際結果是
  `worker-ready-frame-missing`（Worker 在 READY 前結束）。Gateway 沒有建立有效
  listener，啟動程序已結束，未留下 Gateway／Official Worker process 或 listener。
- `ceContacted: false`、`requestExecuted: false`；本次沒有 WhoAmI、connection
  validation、fee read、write、Action、Function 或任意 CRM request。

## 判定與邊界

這不是 canonical URI、manifest hash、profile input shape、named-pipe 連線或本機 listener
的 No-Go；目前尚不能由這個 sanitized 結果區分「Credential Manager blob／帳密不可用」、
「帳號未獲 CE Organization 授權」、「IFD home realm／Organization mapping 不正確」、
「CE/ADFS network authentication 失敗」或 Worker client startup 的其他 runtime failure。
不得猜測或把任一候選原因寫入設定，也不得把 `readiness: go` 當成真機相容性證據。

P6 維持 `in_progress`。P7.0、P7.1～P7.5 與 P8 均未啟動；沒有 ChurchReport cutover。

## 下一個必要的 operator action

在同一個 `LENOVO-LEGION\Administrator` Windows 帳號下，逐一確認兩個 Credential
Manager Generic Credential target（`speechmessage.crm82.p62` 與
`speechmessage.crm91.p62`）的帳號／密碼確實是各自 CE IFD 測試帳號，且該帳號在對應
Organization 有登入授權。canonical root URI 已確認寫入，除非外部管理者確認 URI 或
home realm 事實有誤，否則不要再修改 profile。不要把帳密、credential blob、完整 profile
JSON 或原始 Gateway startup stack trace 貼回；只需重新執行保存於
`operator-handoff-p6.2.md` 的 sanitized startup bridge，貼回最後一行 JSON。

在取得新的 `outcome: started` 或明確 sanitized failure category 前，不得宣稱 P6 CE
evidence 已完成，也不得啟動 P7.0。

## 2026-08-06 continuation checkpoint

本輪以既有、同一 Windows user 的 Credential Manager targets 與最新 published Worker
staging material 重新執行 sanitized startup classification；沒有修改現有 profile input、
feature flag、ChurchReport 流量或 CE 資料。

- `crm82`：`outcome: no-go`、`startupClassification: sdk-diagnostic-unavailable`、
  `workerExitCode: 21`。
- `crm91`：`outcome: no-go`、`startupClassification: sdk-unclassified-failure`、
  `workerExitCode: 20`。
- 兩列均為 `operationExecuted: false`、`featureFlagChanged: false`；兩個 Worker 都在
  READY frame 前停止。
- Windows Credential Manager references 已能通過 readiness 並被 Worker 啟動路徑讀取；
  不再把「target 不存在」列為目前 blocker。
- 兩個 IFD home realm 以不帶 credential 的 metadata `GET` 均得到 HTTP response，XML
  metadata 可解析並含 UsernameToken／WS-Security 相關 binding；固定的 MEX、WS-Trust
  1.3 username/mixed、WS-Trust 2005 username/mixed 變體在隔離 Worker 目錄均產生相同
  pre-READY 結果，因此沒有把現有 profile 改成未驗證 URL。

目前 P6.2 仍為 **No-Go／`in_progress`**。使用者已提供兩個 IFD 登入畫面，顯示兩個
版本各自採用的帳號表示法；畫面本身不能證明 Credential Manager target 儲存的帳號欄位
相符，也不能證明 SDK token exchange 可用。因此下一個最小 operator action 是執行
`Test-DynamicsOfficialWorkerCredentialIdentity.ps1`，只貼回它的 sanitized JSON。若兩個
profile 都回報 `matches-operator-provided-ifd-login`，不得再編輯 target、URI 或 home realm；
接著唯一尚待外部確認的是同一帳號的 CE Organization authorization 或 CE／AD FS 管理端
針對 SDK token exchange 的 sanitized failure category。原始帳號、密碼、token、cookie、
endpoint、exception text 與完整 profile 不得貼回。

## 2026-08-06 credential identity checkpoint

操作者在相同 Windows execution identity 下執行去識別化帳號形式比對，結果為：

```json
{"operationExecuted":false,"featureFlagChanged":false,"outcome":"go","schemaVersion":1,"profiles":[{"profileAlias":"crm82","credentialUserNameState":"matches-operator-provided-ifd-login"},{"profileAlias":"crm91","credentialUserNameState":"matches-operator-provided-ifd-login"}]}
```

這排除了 Credential Manager target 內「UserName 表示法與 IFD 登入形式不一致」這個假設，
但不把密碼、token、credential blob 或 CE 授權狀態視為已驗證。依保存的 bounded startup
checkpoint 重新啟動兩個隔離 Worker 後，結果仍為：

```json
[{"profileAlias":"crm82","outcome":"no-go","startupClassification":"sdk-diagnostic-unavailable","workerExitCode":21,"operationExecuted":false,"featureFlagChanged":false},{"profileAlias":"crm91","outcome":"no-go","startupClassification":"sdk-unclassified-failure","workerExitCode":20,"operationExecuted":false,"featureFlagChanged":false}]
```

因此 P6.2 仍未進入 READY；沒有執行 WhoAmI、connection validation 或其他 CE operation。
目前不應再修改 Credential Manager、canonical URI 或 home realm。下一個必要外部事實是：
相同 IFD 帳號確實能進入對應 CE Organization 並具備授權，或由 CE／AD FS owner 提供
去識別化的 SDK token-exchange failure category。

## 2026-08-06 CE Organization authorization confirmation

操作者在 Lenovo Legion、相同 Windows 帳號下，分別開啟 CE 8.2 與 CE 9.1 的正常
Organization 入口（不是只有 AD FS `/adfs/ls` 登入頁），兩個系統都成功載入實際的
Dynamics CE Organization 首頁。這確認同一登入帳號具備兩個對應 Organization 的互動式
存取授權；不包含任何 CE 寫入，也不等同於 Official Worker SDK token exchange 已成功。

去識別化畫面證據由操作者提供，保留於本機暫存位置：

- `C:\Users\Administrator\AppData\Local\Temp\codex-clipboard-f73554d1-b483-4c43-8d2c-b85ba9462391.png`
- `C:\Users\Administrator\AppData\Local\Temp\codex-clipboard-0dfaedd0-2b5d-48d5-852a-d52db550207c.png`

本證據不記錄 URL、帳號、密碼、token、cookie 或頁面 payload。P6.2 仍維持 **No-Go／
`in_progress`**：兩個 Worker 尚未發布 `worker-reported-ready`，因此尚未執行
`runtime.health.whoami`、`runtime.pool.validate.connection` 或任何其他 CE operation。
下一個必要外部事實縮小為 CE／AD FS 管理端針對 SDK username/password token exchange
失敗的去識別化分類或已驗證的設定修正；在此之前不得重跑相同 Worker startup、不得啟動
P7 或 P8。
