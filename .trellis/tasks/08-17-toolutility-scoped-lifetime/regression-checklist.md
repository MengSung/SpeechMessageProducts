# 人工回歸檢查表 —— ToolUtility Scoped 重構（ChurchReport）

補跑 `implement.md` 第 128、180、209 行三個被穿過去的 ⛔ 審查閘門。

**為什麼非做不可**：這次動的是登入會用到的連線生命週期。自動化測試 63 + 11 全部是 mock，
沒有任何一條會建立真實 Dataverse 連線或跑登入流程。前置任務就是在同一類錯誤上造成登入全面失敗。

判定：任一項出現 `ObjectDisposedException`、連線錯誤、或「這一頁的資料是別人的」，即為**失敗**，
記錄畫面與 `CHURCH_REPORT_TRACE.TXT` 內容後停止，不要繼續往下測。

---

## A. 登入（風險最高，先測）

Run 2 改了連線生命週期 —— 登入路徑就是靠它取得 Dataverse 連線。

> 註：原先以為 Run 3-A 會動到登入用的 `GalleryViewModel`，經查該檔案裡持有 Factory 的
> 其實是同檔第 42 行的 `LineBindingViewModel`（B 類殘留，不遷移）。
> 因此 Run 3-A **不會動到登入相關程式碼**，但 A 段仍要測 —— Run 2 已經改過連線生命週期。

- [ ] **A1 帳密登入** —— 一般帳號密碼登入成功，進得了首頁
- [ ] **A2 錯誤密碼** —— 出現正常的錯誤訊息，不是例外畫面
- [ ] **A3 LINE 登入** —— 由 LINE 進入的登入流程走得通
- [ ] **A4 LINE OAuth 登入** —— OAuth 回呼路徑登入成功
- [ ] **A5 登出後再登入** —— 同一瀏覽器登出、重新登入，不會拿到上一次的資料

> 失敗徵兆：登入頁 500、登入後首頁空白、`ObjectDisposedException`、
> 或登入成功但顯示別人的姓名／會友資料。

## B. 追蹤檔（Run 1 的閘門，行 128）

- [ ] **B1** `D:\除錯追蹤\CHURCH_REPORT_TRACE.TXT` 有持續寫入
- [ ] **B2** **沒有重複行** —— 同一筆訊息只出現一次
- [ ] **B3** 連續操作 20～30 個動作後，檔案沒有異常膨脹

> 失敗徵兆：同一行日誌出現 2 次以上（表示 `Trace.Listeners` 被掛了不只一個 listener）。
> 註：`Program.cs:170` 另有一處 `Trace.Listeners.Add`，若出現重複行，先確認是不是它造成的。

## C. 背景工作（Run 1.5 改成自建 DI scope）

這兩個是 fire-and-forget，畫面會**先回應**再背景上傳，所以要等一下再確認結果。

- [ ] **C1 小組週報儲存** —— 按儲存 → 立即出現「資料已送出，正在背景上傳中」→
      等 30 秒後重新整理，資料**真的存進 Dataverse**
- [ ] **C2 個人資料批次上傳** —— 維護個人資訊批次儲存 → 等 30 秒 → 電話／地址真的更新了
- [ ] **C3** 上述兩項操作後，追蹤檔沒有 `ObjectDisposedException`

> 失敗徵兆：畫面說成功但資料沒進去（背景 scope 提早釋放，工作靜默失敗）。
> **這一項一定要等待並回頭確認，光看畫面訊息不算通過。**

## D. QR Code（Run 3-A 改了 4 個 utility）

- [ ] **D1** 一般 QR Code 產生（`QrCodeController`）
- [ ] **D2** 小組 QR Code
- [ ] **D3** 主日 QR Code
- [ ] **D4** 個人 QR Code
- [ ] **D5** 手機綁定頁的 QR Code（`PhoneBindingController`）

## E. 奉獻與付款（Run 3-A 改了 2 個 processor）

- [ ] **E1** 奉獻流程走得完
- [ ] **E2** 費用付款流程走得完
- [ ] **E3** 定期定額付款流程走得完
- [ ] **E4** 付款完成後的 LINE 通知有發出

## F. PRD A8 原列項目

- [ ] **F1** 會友查詢
- [ ] **F2** 會友編輯
- [ ] **F3** 影像上傳
- [ ] **F4** LINE 綁定
- [ ] **F5** 批次下載

## G. 跨請求與併發（這次重構的核心目的）

前面各項是「功能還在嗎」，這一段是「**連線有沒有真的隔離**」—— 也是整個重構的意義所在。

- [ ] **G1 連續操作** —— 同一 session 連續操作 10 次以上不同功能，全程正常
- [ ] **G2 閒置後再操作** —— 登入後閒置 5 分鐘再操作，正常
      （Run 1.5 移除了 30 分鐘的 session 鍵快取，要確認沒有副作用）
- [ ] **G3 多分頁同時操作** —— 開 2～3 個分頁，同時送出不同功能的請求，
      各自拿到**自己的**資料，不會互相污染
- [ ] **G4 兩個不同帳號同時登入** —— 用兩個瀏覽器（或無痕）各登入不同帳號，
      交錯操作，**絕對不能看到對方的資料**

> G3 / G4 失敗就是這次重構最該抓到的問題：連線仍被跨請求共用。

## H. 死碼刪除的回歸（Run 2.5a）

Run 2.5a 刪了 7 個型別（`DedicationInfo`、`EquipmentStatusCalculator`、`HappyGroupUtility`、
`LineBindingUtility`、`RegisterConnector`、`UploadData`、`WebServiceConnector`）與
`RegisterManager`。編譯有過，但若有 view 或設定檔用字串引用，只會在執行期炸。

- [ ] **H1** 走過一輪主要頁面，沒有出現找不到型別／找不到 view 的錯誤
- [ ] **H2** 裝備、名單管理、新人流程頁面正常（這幾個與被刪型別同區域）

---

## 已知殘留（不是缺陷，是這次刻意畫的邊界）

19 個 `ToolUtilityFactory.GetInstance` 呼叫點未遷移，它們仍共用程序級單一連線。
全部卡在 `InMemoryDataContextSmallGroup` 的 13 個 session 鍵快取（30 分鐘）。
**等同重構前的行為，不是退步**，清單見 `notes.md`，後續票見 `followup-session-cache.md`。

若 G3 / G4 出現跨帳號資料污染，先對照這份清單確認是不是落在那 19 處 —— 若是，屬已知殘留；
若不是，代表 A 類遷移有漏，要回頭查。

---

## 回歸結果

- 執行日期：
- 執行者：
- 環境：測試 / 正式
- 結果：全過 / 失敗（第 ___ 項）
- 失敗時的 `CHURCH_REPORT_TRACE.TXT` 節錄：

全過後才可把 task 狀態更新為完成。
