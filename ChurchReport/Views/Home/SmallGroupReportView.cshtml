@{
    String DataStatus = "上傳成功了!";
    String ReUpload = "請再按一次上傳鈕!";
    @*String DateVaule = "";
        @if (@Model.SundayPrayers.Year != 9999)
        {
            DateVaule = @Model.SundayPrayers.ToShortDateString();

        }
        else
        {
            DateVaule = "";

        }*@
}

@using ChurchReport.Models

@model ChurchReport.Models.SmallGroupData

<head>
    <!-- Site made with Mobirise Website Builder v4.0.16, https://mobirise.com -->
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="generator" content="Mobirise v4.0.16, mobirise.com">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="shortcut icon" href="assets/images/logo.png" type="image/x-icon">
    <meta name="description" content="">
    <title>高雄錫安堂</title>

</head>

@if (Model.SundayPrayers.Year != 2000)
{
    <h3>@(Model.SmallGroupLeaderFullName + "回報")</h3>
}
else
{
    <h3>@(Model.SmallGroupLeaderFullName + "尚未回報")</h3>
}


<div class="dx-field">
    <div id="the whole thing" style="height:100%; width:100%; overflow: hidden;">
        <div id="SundayLabel" class="dx-field-label" style="float: left; width:15%; background-color:white;">小組日期</div>
        <div id="dateBoxContainer" class="dx-field-value" style="float: left; width:55%; background-color:white;">

            @(Html.DevExtreme()
                        //.DateBox()
                        .DateBoxFor(m => m.SundayPrayers)
                        .Type(DateBoxType.Date)
                        .Value(Model.SundayPrayers)
                        .Width(50)
                        .DisplayFormat("yyyy/M/d")
                        //.DisplayFormat("yyyy/MM/dd")
                        //.OnValueChanged("OnValueChanged")


                        .OnValueChanged(@<text>
                        function(arg) {

                        $("#loadPanel").dxLoadPanel("instance").show();

                        $.ajax({
                        url: '@Url.Action("UpdateDate", "Home")',
                        data: { SelectedDate: getODataLocalDateFilter(arg.value) },
                        type: 'GET', //POST if you want to save, GET if you want to fetch data from server

                        success: function (arg)
                        {
                        //alert("001");
                        $("#loadPanel").dxLoadPanel("instance").hide();

                        //var dataGridInstance = $("#gridContainer").dxDataGrid("instance");
                        //dataGridInstance.refresh();
                        //alert("002");

                        window.location.href = "/Home/SmallGroupReportView/AccountPassword"
                        //alert("003");

                        },

                        error: function (obj) {
                        //alert('Something happened');
                        }

                        });
                        }
                        </text>
                                           )

            )
        </div>
        <div id="buttonContainer" class="dx-button-icon" style="float: left; width:30%; text-align: left; background-color:white;">
            @(Html.DevExtreme().Button()
                    .Text("上傳")
                    .Type(ButtonType.Success)
                    .ID("save-button")
                    //.UseSubmitBehavior(true)

                    .OnClick
                    (@<text>
                    function(arg)
                    {

                    var dataGrid = $("#gridContainer").dxDataGrid("instance");
                    // 取得上傳的按鈕
                    var ButtonContainer= $("#save-button").dxButton("instance");

                    //debugger;
                    var Flag = dataGrid.hasEditData();//是否還殘留尚未回傳的編輯資料

                    if(Flag == false)
                    {
                    // 顯示旋轉鈕
                    $("#loadPanel").dxLoadPanel("instance").show();

                    // 上傳按鈕反白
                    ButtonContainer.option("disabled", true);

                    // AJAX 區塊
                    $.ajax
                    (
                    {
                    url: '@Url.Action("SaveSmallGroup", "Home")',
                    data: { aResult: GetResult() }, // 整理回傳給 Server 的點名資料
                    type: 'POST', //POST if you want to save, GET if you want to fetch data from server

                    success: function (arg)
                    {
                    // 隱藏旋轉鈕
                    $("#loadPanel").dxLoadPanel("instance").hide();

                    // 上傳按鈕恢復可運作
                    ButtonContainer.option("disabled", false);

                    // 顯示上傳成功
                    $("#toast").dxToast("instance").show();
                    },

                    error: function (obj)
                    {
                    //alert('Something happened');
                    }

                    }
                    );
                    // AJAX 區塊
                    }
                    else
                    {

                    // 隱藏旋轉鈕
                    $("#loadPanel").dxLoadPanel("instance").hide();
                    // 顯示再上傳一次
                    $("#ToastReUpload").dxToast("instance").show();

                    }
                    }
                    </text>)
            )
        </div>
        <div id="ToastContainer">
            @(Html.DevExtreme().Toast()
                    .ID("toast")
                    .Position(p => p
                    .At(HorizontalAlignment.Center, VerticalAlignment.Center)
                    .Offset(0, 50))
                    .Message(DataStatus)
                    .DisplayTime(2000)
                    .MaxWidth(200)
            )

        </div>
        <div id="LoadPanel">
            @(Html.DevExtreme().LoadPanel()
                    .ID("loadPanel")
                    .ShadingColor("rgba(0,0,0,0.4)")
                    //.Position(p => p.Of("#data-grid").Of("#buttonContainer"))
                    .Position(p => p.Of("#data-grid"))
                    .Visible(false)
                    .ShowIndicator(true)
                    .ShowPane(true)
                    .Shading(true)
                    .CloseOnOutsideClick(false)
                    .Message("更新資料中...")
                    .Animation(l => l.Show(k => k.Easing(AnimationEasing.Swing)))
                    //.OnShown("loadPanel_shown")
                    //.OnHidden("loadPanel_hidden")
            )
        </div>
        <div id="ToastContainer">
            @(Html.DevExtreme().Toast()
                        .ID("ToastReUpload")
                        .Position(p => p
                        .At(HorizontalAlignment.Center, VerticalAlignment.Center)
                        .Offset(0, 50))
                        .Message(ReUpload)
                        .DisplayTime(5000)
                        .Type(ToastType.Error)
                        .MaxWidth(500)
            )

        </div>
    </div>
</div>

<hr />
<h3>牧養</h3>

@if (Model.LoginType == "小組長")
{
    <div id="data-grid">
        @(Html.DevExtreme().DataGrid<Member>()
                        .ID("gridContainer")
                        .ShowBorders(true)
                        .ColumnAutoWidth(true)
                        .Paging(p => p.PageSize(10))
                        .Paging(paging => paging.Enabled(false))
                        .AllowColumnResizing(true)
                        .ColumnResizingMode(ColumnResizingMode.Widget)
                        .ColumnFixing(c => c.Enabled(true))
                        .LoadPanel(l => l.Text("更新資料中..."))
                        .SearchPanel(s => s.Visible(true).Placeholder("搜尋.."))


                        .StateStoring(s => s
                        .Enabled(true)
                        .Type(StateStoringType.LocalStorage)
                        .StorageKey("FullName")
                        )
                        //.StateStoring(s => s
                        //    .Enabled(true)
                        //    .Type(StateStoringType.LocalStorage)
                        //    .StorageKey("Id")
                        //)

                        //.OnRowUpdated("function () { alert('OnRowUpdated'); }")
                        //.OnRowClick("function () { alert('OnRowClick'); }")
                        //.OnSelectionChanged("function () { alert('OnSelectionChanged'); }" )
                        //.OnCellClick("function () { alert('OnCellClick'); }")
                        //.OnEditingStart("function () { alert('OnEditingStart'); }")
                        //.OnEditorPrepared("function () { alert('OnEditorPrepared'); }")




                        .ShowColumnLines(true)
                        .ShowRowLines(true)
                        .RowAlternationEnabled(true)
                        .ShowBorders(true)

                        .TwoWayBindingEnabled(true)

                        .Grouping
                        (g => g
                        .ContextMenuEnabled(true)
                        .ExpandMode(GridGroupingExpandMode.RowClick)
                        )

                        .GroupPanel
                        (p => p
                        .EmptyPanelText("請把小組名稱拉到這裡")
                        .Visible(false) // 讓使用者不能自己決定誰被Grouping起來
                        .AllowColumnDragging(true)
                        )

                        //.GroupPanel(p => p.Visible(true))
                        //.GroupPanel(p => p.AllowColumnDragging(false))
                        //.GroupPanel(p => p.(false))
                        //.Grouping(g => g.AutoExpandAll(true))


                        .Editing(editing =>
                        {
                            //editing.Mode(GridEditMode.Batch);
                            //editing.Mode(GridEditMode.Row);
                            editing.Mode(GridEditMode.Cell);
                            //editing.Mode(GridEditMode.Batch);
                            //editing.Mode(GridEditMode.Form);
                            //editing.Mode(GridEditMode.Popup);
                            editing.AllowUpdating(true);
                            editing.AllowDeleting(false);
                            //editing.AllowAdding(false);
                            editing.Texts(a => a.SaveAllChanges("上傳檔案").AddRow("新增").CancelAllChanges("取消變更").SaveRowChanges("儲存").CancelRowChanges("取消").EditRow("編輯").DeleteRow("刪除"));
                            //editing.AllowDeleting(true);
                        })

                .Columns(columns =>
                {
                    //columns.Add().DataField("FullName").Caption("姓名").Fixed(true).Width(80).MinWidth(20).Alignment(HorizontalAlignment.Left);
                    //columns.Add().DataField("Status").Caption("委身類型").Fixed(false).Width(100).MinWidth(20).Alignment(HorizontalAlignment.Center);
                    //columns.Add().DataField("Sunday").Caption("主日").DataType(GridColumnDataType.Boolean).Width(45).MinWidth(45);
                    //columns.Add().DataField("SmallGroup").Caption("小組").DataType(GridColumnDataType.Boolean).Width(45).MinWidth(45);
                    //columns.Add().DataField("PrayItem").Caption("代禱事項").Fixed(false).MinWidth(90);
                    //columns.Add().DataField("SmallGroupName").Caption("小組").GroupIndex(0).MinWidth(10);

                    columns.AddFor(m => m.FullName).Caption("姓名").Fixed(true).Width(80).MinWidth(20).Alignment(HorizontalAlignment.Left);
                    columns.AddFor(m => m.Status).Caption("委身類型").Fixed(false).Width(100).MinWidth(20).Alignment(HorizontalAlignment.Center);
                    columns.AddFor(m => m.Sunday).Caption("主日").Width(45).MinWidth(45);
                    columns.AddFor(m => m.SmallGroup).Caption("小組").Width(45).MinWidth(45);
                    columns.AddFor(m => m.PrayItem).Caption("代禱事項").MinWidth(90);
                    columns.AddFor(m => m.SmallGroupName).Caption("小組").GroupIndex(0).MinWidth(10);

                })
                            .DataSource(Model.Members)
        )
    </div>
}
else
{
    <div id="data-grid">
        @(Html.DevExtreme().DataGrid<Member>()
                        .ID("gridContainer")
                        .ShowBorders(true)
                        .ColumnAutoWidth(true)
                        .Paging(p => p.PageSize(10))
                        .Paging(paging => paging.Enabled(false))
                        .AllowColumnResizing(true)
                        .ColumnResizingMode(ColumnResizingMode.Widget)
                        .ColumnFixing(c => c.Enabled(true))
                        .LoadPanel(l => l.Text("更新資料中..."))
                        .SearchPanel(s => s.Visible(true).Placeholder("搜尋.."))


                        .StateStoring(s => s
                        .Enabled(true)
                        .Type(StateStoringType.LocalStorage)
                        .StorageKey("FullName")
                        )
                        //.StateStoring(s => s
                        //    .Enabled(true)
                        //    .Type(StateStoringType.LocalStorage)
                        //    .StorageKey("Id")
                        //)

                        //.OnRowUpdated("function () { alert('OnRowUpdated'); }")
                        //.OnRowClick("function () { alert('OnRowClick'); }")
                        //.OnSelectionChanged("function () { alert('OnSelectionChanged'); }" )
                        //.OnCellClick("function () { alert('OnCellClick'); }")
                        //.OnEditingStart("function () { alert('OnEditingStart'); }")
                        //.OnEditorPrepared("function () { alert('OnEditorPrepared'); }")

                        .OnContentReady(@<text>
                        function(e) {
                        moveEditColumnToLeft(e.component);
                        }
                        </text> )

                                        .OnCellPrepared("cell_prepared")



                                        .ShowColumnLines(true)
                                        .ShowRowLines(true)
                                        .RowAlternationEnabled(true)
                                        .ShowBorders(true)

                                        .TwoWayBindingEnabled(true)

                                        .Grouping
                                        (g => g
                                        .ContextMenuEnabled(true)
                                        .ExpandMode(GridGroupingExpandMode.RowClick)
                                        )

                                        .GroupPanel
                                        (p => p
                                        .EmptyPanelText("請把小組名稱拉到這裡")
                                        .Visible(false) // 讓使用者不能自己決定誰被Grouping起來
                                        .AllowColumnDragging(true)
                                        )

                                        //.GroupPanel(p => p.Visible(true))
                                        //.GroupPanel(p => p.AllowColumnDragging(false))
                                        //.GroupPanel(p => p.(false))
                                        //.Grouping(g => g.AutoExpandAll(true))


                                        .Editing(editing =>
                                        {
                                            //editing.Mode(GridEditMode.Batch);
                                            //editing.Mode(GridEditMode.Row);
                                            //editing.Mode(GridEditMode.Cell);
                                            //editing.Mode(GridEditMode.Batch);
                                            //editing.Mode(GridEditMode.Form);
                                            editing.Mode(GridEditMode.Popup);
                                            editing.AllowUpdating(true);
                                            editing.AllowDeleting(false);
                                            //editing.AllowAdding(false);
                                            editing.Texts(a => a.SaveAllChanges("上傳檔案").AddRow("新增").CancelAllChanges("取消變更").SaveRowChanges("儲存").CancelRowChanges("取消").EditRow("編輯").DeleteRow("刪除"));
                                            //editing.AllowDeleting(true);
                                        })

                                .Columns(columns =>
                                {
                                    /**/
                        @*columns.Add()
                            .DataField("Picture")
                            .Caption("照片")
                            .Width(60)
                            .AllowFiltering(false)
                            .AllowSorting(false)
                            .CellTemplate(@<text>
                            <div class="img-container">
                                <img src="<%= value %>" alt="" />
                            </div>
                            </text>        );*@
                                    /**/


                                    //columns.Add()
                                    //    .Caption("測試")
                                    //    //.DataField("Shepherd")
                                    //    .Width(250)
                                    //
                                    //    .CellTemplate(@<text> @(Html.Partial("DropDownBox") )
                                    //    </text>);

                                    //columns.Add().DataField("FullName").Caption("姓名").Fixed(true).Width(80).MinWidth(20).Alignment(HorizontalAlignment.Left);
                                    //columns.Add().DataField("Status").Caption("委身類型").Fixed(false).Width(100).MinWidth(20).Alignment(HorizontalAlignment.Center);
                                    //columns.Add().DataField("Sunday").Caption("主日").DataType(GridColumnDataType.Boolean).Width(45).MinWidth(45);
                                    //columns.Add().DataField("SmallGroup").Caption("小組").DataType(GridColumnDataType.Boolean).Width(45).MinWidth(45);
                                    //columns.Add().DataField("PrayItem").Caption("代禱事項").Fixed(false).MinWidth(90);
                                    //columns.Add().DataField("SmallGroupName").Caption("小組").GroupIndex(0).MinWidth(10);

                                    columns.AddFor(m => m.FullName).Caption("姓名").Fixed(true).Width(80).MinWidth(20).Alignment(HorizontalAlignment.Left);
                                    columns.AddFor(m => m.Status).Caption("委身類型").Fixed(false).Width(100).MinWidth(20).Alignment(HorizontalAlignment.Center);
                                    columns.AddFor(m => m.Sunday).Caption("主日").Width(45).MinWidth(45);
                                    columns.AddFor(m => m.SmallGroup).Caption("小組").Width(45).MinWidth(45);
                                    columns.AddFor(m => m.PrayItem).Caption("代禱事項").MinWidth(90);
                                    columns.AddFor(m => m.SmallGroupName).Caption("小組").GroupIndex(0).MinWidth(10);

                                    //columns.Add()
                                    //    .DataField("StateID1")
                                    //    .Caption("牧養活動")
                                    //    .Width(80).MinWidth(30)
                                    //    .Lookup(lookup => lookup
                                    //    .DataSource(d => d.WebApi().Controller("ShepherdMethodLookup").LoadAction("Get").Key("ID"))
                                    //    .DisplayExpr("Name")
                                    //    .ValueExpr("ID")
                                    //);

                                    //columns.Add()
                                    //    .Caption("牧養次數")
                                    //    //.DataField("Shepherd")
                                    //    .Width(80)
                                    //    .CellTemplate(@<text> @(Html.DevExtreme().NumberBox()
                                    //    .Value(0)
                                    //    .Min(0)
                                    //    .Max(20)
                                    //    .ShowSpinButtons(true) )
                                    //    </text>        );

                                    //columns.Add().DataField("Number1").Caption("次數").DataType(GridColumnDataType.Number).Width(50).MinWidth(10);
                                    //
                                    //columns.Add()
                                    //    .DataField("StateID2")
                                    //    .Caption("牧養活動")
                                    //    .Width(80).MinWidth(30)
                                    //    .Lookup(lookup => lookup
                                    //    .DataSource(d => d.WebApi().Controller("ShepherdMethodLookup").LoadAction("Get").Key("ID"))
                                    //    .DisplayExpr("Name")
                                    //    .ValueExpr("ID")
                                    //);
                                    //
                                    //columns.Add().DataField("Number2").Caption("次數").DataType(GridColumnDataType.Number).Width(50).MinWidth(10);


                                })
                                            //.DataSource(d => d.WebApi().Controller("SmallGroupReport").Key("Id"))
                                            .DataSource(Model.Members)
        //.DataSourceOptions(d => d.Filter("['Id', '<=' , 6]"))
        )
    </div>
}
<script>var DataUploadStatus = "尚未上傳喔!";</script>
<script src="~/js/DataGridAjax.js"></script>
<script src="~/js/SelectDate.js"></script>
<script src="~/js/DropDownBox.js"></script>
<script src="~/js/DataGridAjax.js"></script>
<script src="~/js/LoadPanel.js"></script>
<script>
    function moveEditColumnToLeft(dataGrid) {
        dataGrid.columnOption("command:edit", {
            visibleIndex: -1,
            width: 50
        });
    }

    function cell_prepared(e) {
        if (e.rowType === "data" && e.column.command === "edit") {
            var isEditing = e.row.isEditing,
                $links = e.cellElement.find(".dx-link");

            $links.text("");

            if (isEditing) {
                $links.filter(".dx-link-save").addClass("dx-icon-save");
                $links.filter(".dx-link-cancel").addClass("dx-icon-revert");
            } else {
                $links.filter(".dx-link-edit").addClass("dx-icon-edit");
                $links.filter(".dx-link-delete").addClass("dx-icon-trash");
            }
        }
    }
</script>
<link href="~/css/SmallGroupReportController.css" rel="stylesheet" />

