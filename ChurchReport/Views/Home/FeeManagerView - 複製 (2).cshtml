@{
    String DataStatus = "上傳成功了!";
    String ReUpload = "請再按一次上傳鈕!";
}

<head>
    <!-- Site made with Mobirise Website Builder v4.0.16, https://mobirise.com -->
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="generator" content="Mobirise v4.0.16, mobirise.com">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="shortcut icon" href="assets/images/logo.png" type="image/x-icon">
    <meta name="description" content="">
    <title>從懷疑到相信</title>
</head>

<br />
<br />
<div style="font-family:標楷體;font-size: 120%; color:crimson; text-align:left;background-color:powderblue;font-weight: bold">一對一進度請按"編輯"填入</div>
<br />

<div class="dx-field">
    <div id="SaveButton" style="height:100%; width:100%; overflow: hidden;">
        <div id="buttonContainer" class="dx-button-icon" style="float: left; width:30%; text-align: left; background-color:white;">
            @(Html.DevExtreme().Button()
                .Text("上傳")
                .Type(ButtonType.Success)
                .ID("save-button")
                .Visible(false)
                //.UseSubmitBehavior(true)

                .OnClick
                (@<text>
                    function(arg)
                    {
                    var dataGrid = $("#gridContainer").dxDataGrid("instance");
                    // 取得上傳的按鈕
                    var ButtonContainer= $("#save-button").dxButton("instance");

                    //debugger;
                    var Flag = dataGrid.hasEditData();

                    if(Flag == false)
                    {
                    // 顯示旋轉鈕
                    $("#loadPanel").dxLoadPanel("instance").show();

                    // 上傳按鈕反白
                    ButtonContainer.option("disabled", true);

                    // AJAX 區塊
                    $.ajax
                    (
                    {
                    url: '@Url.Action("UploadReportDiscipleLessonsList", "Home")',
                    data: { aResult: "工人回報上課" }, // 整理回傳給 Server 的點名資料
                    type: 'POST', //POST if you want to save, GET if you want to fetch data from server

                    success: function (arg)
                    {
                    // 隱藏旋轉鈕
                    $("#loadPanel").dxLoadPanel("instance").hide();

                    // 上傳按鈕恢復可運作
                    ButtonContainer.option("disabled", false);

                    // 顯示上傳成功
                    $("#toast").dxToast("instance").show();
                    },

                    error: function (obj)
                    {
                    //alert('Something happened');
                    }

                    }
                    );
                    // AJAX 區塊
                    }
                    else
                    {

                    // 隱藏旋轉鈕
                    $("#loadPanel").dxLoadPanel("instance").hide();
                    // 顯示再上傳一次
                    $("#ToastReUpload").dxToast("instance").show();

                    }
                    }
                </text>)
            )
        </div>
        <div id="ToastContainer">
            @(Html.DevExtreme().Toast()
                .ID("toast")
                .Position(p => p
                .At(HorizontalAlignment.Center, VerticalAlignment.Center)
                .Offset(0, 50))
                .Message("上傳成功了!")
                .DisplayTime(2000)
                .Width(300)
                //.Height(200)
                .MaxWidth(350)
            )
        </div>
        <div id="LoadPanel">
            @(Html.DevExtreme().LoadPanel()
                .ID("loadPanel")
                .ShadingColor("rgba(0,0,0,0.4)")
                //.Position(p => p.Of("#data-grid").Of("#buttonContainer"))
                .Position(p => p.Of("#data-grid"))
                .Visible(false)
                .ShowIndicator(true)
                .ShowPane(true)
                .Shading(true)
                .CloseOnOutsideClick(false)
                .Message("更新資料中...")
                .Animation(l => l.Show(k => k.Easing(AnimationEasing.Swing)))
                //.OnShown("loadPanel_shown")
                //.OnHidden("loadPanel_hidden")
            )
        </div>
        <div id="ToastContainer">
            @(Html.DevExtreme().Toast()
                .ID("ToastReUpload")
                .Position(p => p
                .At(HorizontalAlignment.Center, VerticalAlignment.Center)
                .Offset(0, 50))
                .Message("耶和華")
                .DisplayTime(5000)
                .Type(ToastType.Error)
                .MaxWidth(500)
            )

        </div>
    </div>
</div>

<div id="data-grid">
    @(Html.DevExtreme().DataGrid()
                .ID("gridContainer")
                .ShowBorders(true)
                //.ColumnAutoWidth(true)
                //.Paging(p => p.PageSize(10))
                .Paging(paging => paging.Enabled(false))
                .AllowColumnResizing(true)
                //.ColumnResizingMode(ColumnResizingMode.Widget)
                .ColumnFixing(c => c.Enabled(true))
                .LoadPanel(l => l.Text("更新資料中..."))
                //.SearchPanel(s => s.Visible(true).Placeholder("搜尋.."))

                .DataSource(d => d
                .WebApi()
                //.Mvc()
                .Controller("Home")
                .LoadAction("LoadWeekdayOptionList")
                //.InsertAction("AddClassSheet")
                .UpdateAction("UpdateClassSheetReport")
                .Key("StorLessonsId")
                .LoadParams(new { ActiveType = "Active!" })
                )



                //.StateStoring(s => s
                //.Enabled(true)
                //.Type(StateStoringType.LocalStorage)
                //.StorageKey("FullName")
                //)

                .ShowColumnLines(true)
                .ShowRowLines(true)
                //.RowAlternationEnabled(true)
                .ShowBorders(true)

                .TwoWayBindingEnabled(true)

                //.Grouping
                //(g => g
                //.ContextMenuEnabled(true)
                //.ExpandMode(GridGroupingExpandMode.RowClick)
                //)

                .Editing
                (editing =>
                {
                    //editing.Mode(GridEditMode.Batch);
                    //editing.Mode(GridEditMode.Row);
                    //editing.Mode(GridEditMode.Cell);
                    //editing.Mode(GridEditMode.Batch);
                    //editing.Mode(GridEditMode.Form);
                    editing.Mode(GridEditMode.Popup);
                    editing.AllowUpdating(true);
                    //editing.AllowDeleting(false);
                    //editing.AllowAdding(false);
                    editing.Texts(a => a.SaveAllChanges("上傳檔案").AddRow("新增").CancelAllChanges("取消變更").SaveRowChanges("儲存").CancelRowChanges("取消").EditRow("編輯").DeleteRow("刪除"));
                    //editing.AllowDeleting(true);
                    editing.Form
                    (a => a.ColCount(2).Items
                    (RootItems =>
                    {
                        //RootItems.AddGroup().Items
                        //( groupItems =>
                        //{
                        // //groupItems.AddSimpleFor(m => m.ID).IsRequired(false);
                        // //groupItems.AddSimpleFor(m => m.FirstName);
                        // //groupItems.AddSimpleFor(m => m.LastName);
                        // //groupItems.AddSimpleFor(m => m.HireDate).IsRequired(false);
                        // //groupItems.AddSimpleFor(m => m.Position);
                        // //groupItems.AddSimpleFor(m => m.OfficeNo);
                        //}
                        //);
                        //RootItems.AddEmpty()
                        //rootItems.AddGroup()
                        // .Caption("System Information")
                        // .Items(groupItems =>
                        // {
                        // //groupItems.AddSimpleFor(m => m.ID).IsRequired(false);
                        // //groupItems.AddSimpleFor(m => m.FirstName);
                        // //groupItems.AddSimpleFor(m => m.LastName);
                        // //groupItems.AddSimpleFor(m => m.HireDate).IsRequired(false);
                        // //groupItems.AddSimpleFor(m => m.Position);
                        // //groupItems.AddSimpleFor(m => m.OfficeNo);
                        // });
                    }
                    )
                    .CustomizeItem("CustomPopupForm")
                    );
                }
                )


                // 此處設定是讓選項偏左邊一排
                .OnContentReady(
                @<text>
                    function(e)
                    {
                    moveEditColumnToLeft(e.component);
                    //ShowToast( ReMindMessage , 'warning' , 20000)
                    }
                </text>
                        )

                                        .Columns(columns =>
                                        {
                                            columns.Add().DataField("FullName").Caption("姓名").Fixed(true).Width(80).MinWidth(20).Alignment(HorizontalAlignment.Left);

                                            columns.Add().DataField("DiscipleLessonsStatusCode").Caption("狀態").GroupIndex(0).AllowEditing(false).Width(80).MinWidth(20).Alignment(HorizontalAlignment.Left);

                                            columns.Add().DataField("HomeWorkA").DataType(GridColumnDataType.Date).Caption("作業A").Format("yyyy/M/d")
                                .CalculateCellValue(@<text>
                                    function(row)
                                    {
                                    if( row.HomeWorkA != null )
                                    {
                                    //debugger;

                                    var HomeWorkA = ParsingDate( row.HomeWorkA );
                                    if( HomeWorkA.getFullYear() == 1901 )
                                    {
                                    return null;
                                    }
                                    else
                                    {
                                    return HomeWorkA;
                                    }
                                    }
                                    else
                                    {return null;}
                                    }
                                </text> )
                                .Fixed(false).Width(120).MinWidth(120);


                                            columns.Add().DataField("HomeWorkB").DataType(GridColumnDataType.Date).Caption("作業B").Format("yyyy/M/d")
                                .CalculateCellValue(@<text>
                                    function(row)
                                    {
                                    if( row.HomeWorkB != null )
                                    {
                                    //debugger;

                                    var HomeWorkB = ParsingDate( row.HomeWorkB );
                                    if( HomeWorkB.getFullYear() == 1901 )
                                    {
                                    return null;
                                    }
                                    else
                                    {
                                    return HomeWorkB;
                                    }
                                    }
                                    else
                                    {return null;}
                                    }
                                </text> )
                                .Fixed(false).Width(120).MinWidth(120);

                                        })


                                    //.DataSource(d => d
                                    // .WebApi()
                                    // .Controller("Home")
                                    // .LoadAction("LoadClassSheetReport")
                                    // //.InsertAction("AddClassSheet")
                                    // //.UpdateAction("UpdateClassSheet")
                                    // .Key("DiscipleLessonsId")
                                    // .LoadParams(new { ActiveType = "Active!" })
                                    //)

                                    //.DataSource(Model.m_ReportDiscipleLessonsList)
    )
</div>

<script>var DataUploadStatus = "尚未上傳喔!";</script>
<script src="~/js/DataGridAjax.js"></script>
<script src="~/js/SelectDate.js"></script>
<script src="~/js/DropDownBox.js"></script>
<script src="~/js/DataGridAjax.js"></script>
<script src="~/js/LoadPanel.js"></script>
<script>


function moveEditColumnToLeft(dataGrid) {
dataGrid.columnOption("command:edit", {
visibleIndex: -1,
width: 50
});
}
function cell_prepared(e) {
if (e.rowType === "data" && e.column.command === "edit") {
var isEditing = e.row.isEditing,
$links = e.cellElement.find(".dx-link");

$links.text("");

if (isEditing) {
$links.filter(".dx-link-save").addClass("dx-icon-save");
$links.filter(".dx-link-cancel").addClass("dx-icon-revert");
} else {
$links.filter(".dx-link-edit").addClass("dx-icon-edit");
$links.filter(".dx-link-delete").addClass("dx-icon-trash");
}
}
}
function cell_prepared_background_color(e) {
//if (e.rowType === "data" && e.column.command === "edit") {
// var isEditing = e.row.isEditing,
// $links = e.cellElement.find(".dx-link");

// $links.text("");

// if (isEditing) {
// $links.filter(".dx-link-save").addClass("dx-icon-save");
// $links.filter(".dx-link-cancel").addClass("dx-icon-revert");
// } else {
// $links.filter(".dx-link-edit").addClass("dx-icon-edit");
// $links.filter(".dx-link-delete").addClass("dx-icon-trash");
// }
//}

//debugger;
if (e.rowType == "data" && e.column.dataField === 'OneThirdReport') {

e.cellElement.css('background', '#FF5733');
//e.cellElement.css('background', 'red');
//if (e.data.OneThirdReport === 'Male') { e.cellElement.addClass('Red'); }
//else { e.cellElement.addClass('Blue'); }
//if (e.data.OneThirdReport === 'Female') { e.cellElement.addClass('Pink'); }
}
if (e.rowType == "data" && e.column.dataField === 'FourSixReport') {

e.cellElement.css('background', '#FF5733');
//e.cellElement.css('background', 'red');
//if (e.data.OneThirdReport === 'Male') { e.cellElement.addClass('Red'); }
//else { e.cellElement.addClass('Blue'); }
//if (e.data.OneThirdReport === 'Female') { e.cellElement.addClass('Pink'); }
}
if (e.rowType == "data" && e.column.dataField === 'SevenTenReport') {

e.cellElement.css('background', '#FF5733');
//e.cellElement.css('background', 'red');
//if (e.data.OneThirdReport === 'Male') { e.cellElement.addClass('Red'); }
//else { e.cellElement.addClass('Blue'); }
//if (e.data.OneThirdReport === 'Female') { e.cellElement.addClass('Pink'); }
}
}

function GetCustomizeText(rowData) {
if (rowData.FirstClassDate != null) {
if (rowData.FirstClassDate.indexOf("0001-01-01T00:00:00") >= 0 ) {
return "";
}
else {
return rowData.FirstClassDate;
}
}
}
function ParsingDate(input) {

    debugger;
    if( typeof(input) === 'string' )
    {
         ////alert("ParseDate 001 : " + input);
        var parts = input.split('-');

        var DaySplitedArray = parts[2].split('T');
        var ParsedDate = new Date(parts[0], parts[1] - 1, DaySplitedArray[0]);
        //var ParsedDate = new Date(parts[0], parts[1], DaySplitedArray[0]);

        return ParsedDate;
   }
    else{
         debugger;
        return getODataLocalDateFilter(input);
    }
}

 function getODataLocalDateFilter(date) {

        //debugger;

        //alert("getODataLocalDateFilter !" + date);
        //alert("HELLO 我愛嘟嘟妞妞 006!" + date);

        //-- Description: For converting the date object to local time format
        //-- You can also convert this to UTC Date format
        //-- UTC Usage: getUTCMonth(), getUTCFullYear(), getUTCHours() ...

        var monthString;
        var rawMonth = (date.getMonth() + 1).toString();
        if (rawMonth.length == 1) {
            monthString = "0" + rawMonth;
        }
        else { monthString = rawMonth; }

        var dateString;
        //var rawDate = date.getUTCDate().toString();
        var rawDate = date.getDate().toString();
        if (rawDate.length == 1) {
            dateString = "0" + rawDate;
        }
        else { dateString = rawDate; }


        var DateFilter = "";
        //DateFilter += date.getFullYear() + "-";
        //DateFilter += monthString + "-";
        //DateFilter += dateString;
        //DateFilter += " T" + date.getHours() + ":";
        //DateFilter += date.getMinutes() + ":";
        //DateFilter += date.getSeconds() + ":";
        //DateFilter += date.getMilliseconds();

        //return DateFilter;

        //return date.getFullYear() + "/" + rawMonth + "/" + rawDate;

        return new Date(date.getFullYear(), rawMonth, rawDate );
    }

function CustomPopupForm(Item) {
if (Item.dataField == "OneThirdReport" || Item.dataField == "FourSixReport" || Item.dataField == "SevenTenReport") {
//Item(a => a.css('background-color', '#FF5733'));
//debugger;

// "background-color", "green"
//Item.css('background-color', '#FF5733');

//Item.readOnly = true;
Item.cssClass = "popup-element-background-color";
}
}

var ToastMessage = "我愛嘟嘟妞妞";
var ReMindMessage = "";
var Class1 = "";
var Class2 = "";
var Class3 = "";
var Class4 = "";
var Class5 = "";
var Class6 = "";
var Class7 = "";
var Class8 = "";
var Class9 = "";
var Class10 = "";


function ShowToast(LocalToastMessage, Type, DisplayTime) {
var ToastInstance = $("#toast").dxToast("instance");
//ToastInstance.option('message', ToastMessage);
//ToastInstance.option('message', ReMindMessage);
//ToastInstance.option('message', "我愛嘟嘟妞妞");
ToastInstance.option('message', LocalToastMessage);
ToastInstance.option('type', Type);
ToastInstance.option('displayTime', DisplayTime);
ToastInstance.option('white-space', 'pre-line');
ToastInstance.option('closeOnClick', true);
ToastInstance.show();
}
</script>
<link href="~/css/PopupForm.css" rel="stylesheet" />
