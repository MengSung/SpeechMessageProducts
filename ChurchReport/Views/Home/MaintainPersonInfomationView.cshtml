@{
    String DataStatus = "上傳成功了!";
    String ReUpload = "請再按一次上傳鈕!";
}


@model ChurchReport.Models.SmallGroupData

<head>
    <!-- Site made with Mobirise Website Builder v4.0.16, https://mobirise.com -->
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="generator" content="Mobirise v4.0.16, mobirise.com">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="shortcut icon" href="assets/images/logo.png" type="image/x-icon">
    <meta name="description" content="">
    <title>台北得勝靈糧堂</title>

</head>

@*<br />換行*@
<br />

<div class="dx-field" style="height:100%; width:100%; overflow: auto;">
    <div id="ToastContainer">
        @(Html.DevExtreme().Toast()
                .ID("toast")
                .Position(p => p
                .At(HorizontalAlignment.Center, VerticalAlignment.Center)
                .Offset(0, 50))
                .Message(DataStatus)
                .DisplayTime(2000)
                .MaxWidth(200)
            )

    </div>
    <div id="LoadPanel">
        @(Html.DevExtreme().LoadPanel()
                .ID("loadPanel")
                .ShadingColor("rgba(0,0,0,0.4)")
                .Position(p => p.Of("#data-grid"))
                .Visible(false)
                .ShowIndicator(true)
                .ShowPane(true)
                .Shading(true)
                .CloseOnOutsideClick(false)
                .Message("更新資料中...")
                .Animation(l => l.Show(k => k.Easing(AnimationEasing.Swing)))
                //.OnShown("loadPanel_shown")
                //.OnHidden("loadPanel_hidden")
            )
    </div>
    <div id="ToastContainer">
        @(Html.DevExtreme().Toast()
                    .ID("ToastReUpload")
                    .Position(p => p
                    .At(HorizontalAlignment.Center, VerticalAlignment.Center)
                    .Offset(0, 50))
                    .Message(ReUpload)
                    .DisplayTime(5000)
                    .Type(ToastType.Error)
                    .MaxWidth(500)
            )
    </div>
    @*</div>*@

    <hr />

    <div style="font-family:標楷體;font-size:100%; color:darkcyan; text-align:left;font-weight:bold">
        <h4>個人資料</h4>
    </div>

    <div id="multigroupchart">
        @*<div id="multigroupchart" style="width:800px;height:450px;overflow:auto;">*@
        @(Html.DevExtreme().Map()
        .ID("map")
        .Provider(GeoMapProvider.Google)
        .Center("桃園市楊梅區")
        .Zoom(11)
        .Visible(false)
        //.Width(750)
        //.Width("100%")
        .Width("50%")
        .Height(200)
        @*.Height
          (@<text>
               function()
               {
                    //return window.innerHeight / 1.5;

                    return $("#gridContainer").dxDataGrid("instance").option("height", (($(window).height()) - 20));
               }
        </text>)*@

//.Height("50%")
.Controls(true)
.Type(GeoMapType.Roadmap)
// NOTE: Specify your map API keys for every used provider
.Key(k => k
//.Bing("YOUR_BING_MAPS_API_KEY")
.Google("AIzaSyBNogx0Ziqd3OIEW5b6b9BTJHXHW_YKDlM")
//.GoogleStatic("YOUR_GOOGLE_STATIC_MAPS_API_KEY")
)
.MarkerIconSrc(new JS("markerUrl"))
.OnReady("onReady")
    )
    </div>
    <br />
    <div id="buttonContainer" class="dx-button-icon">
        @(Html.DevExtreme().Button()
            .Text("上傳").Height(50).Width(80).Icon("check")
            .Type(ButtonType.Success)
            .ID("save-button")
            //.UseSubmitBehavior(true)

            .OnClick
            (@<text>
                        function(arg)
                        {

                        var GroupDataGrid = $("#gridContainer").dxDataGrid("instance");

                        // 取得上傳的按鈕
                        var ButtonContainer= $("#save-button").dxButton("instance");

                        //debugger;
                        var GroupDataGridFlag = GroupDataGrid.hasEditData();//是否還殘留尚未回傳的編輯資料

                        if( GroupDataGridFlag == false)
                        {
                        // 顯示旋轉鈕
                        $("#loadPanel").dxLoadPanel("instance").show();

                        // 上傳按鈕反白
                        ButtonContainer.option("disabled", true);

                        // AJAX 區塊
                        $.ajax
                        (
                        {
                        url: '@Url.Action("SavePersonInfomation", "Home")',
                        data: { aResult: GetResult() }, // 整理回傳給 Server 的點名資料
                        type: 'POST', //POST if you want to save, GET if you want to fetch data from server

                        success: function (arg)
                        {
                        // 隱藏旋轉鈕
                        $("#loadPanel").dxLoadPanel("instance").hide();

                        // 上傳按鈕恢復可運作
                        ButtonContainer.option("disabled", false);

                        // 顯示上傳成功
                        $("#toast").dxToast("instance").show();
                        },

                        error: function (obj)
                        {
                        //alert('Something happened');
                        }

                        }
                        );
                        // AJAX 區塊
                        }
                        else
                        {

                        // 隱藏旋轉鈕
                        $("#loadPanel").dxLoadPanel("instance").hide();
                        // 顯示再上傳一次
                        $("#ToastReUpload").dxToast("instance").show();

                        }
                        }

            </text>)
        )
    </div>

    @if (Model.LoginType == "領袖")
    {
        <div id="data-grid">
            @(Html.DevExtreme().DataGrid()
            .ID("gridContainer")
            .ShowBorders(true)
            //.ColumnAutoWidth(true)
            //.Paging(p => p.PageSize(10))
            .Paging(paging => paging.Enabled(false))
            .AllowColumnResizing(true)
            .ColumnResizingMode(ColumnResizingMode.Widget)
            .ColumnFixing(c => c.Enabled(true))
            .LoadPanel(l => l.Text("更新資料中...").Enabled(false))
            .SearchPanel(s => s.Visible(false).Placeholder("搜尋.."))

            .Export(e => e.Enabled(false).AllowExportSelectedData(true))
            //.StateStoring(s => s
            //    .Enabled(true)
            //    .Type(StateStoringType.LocalStorage)
            //    .StorageKey("Id")
            //)

            //.OnRowUpdated("function () { alert('OnRowUpdated'); }")
            //.OnRowClick("function () { alert('OnRowClick'); }")
            //.OnSelectionChanged("function () { alert('OnSelectionChanged'); }" )
            //.OnCellClick("function () { alert('OnCellClick'); }")
            //.OnEditingStart("function () { alert('OnEditingStart'); }")
            //.OnEditorPrepared("function () { alert('OnEditorPrepared'); }")




            .ShowColumnLines(true)
            .ShowRowLines(true)
            .RowAlternationEnabled(true)
            .ShowBorders(true)

            .TwoWayBindingEnabled(true)


            .Grouping
            (g => g
            .ContextMenuEnabled(true)
            .ExpandMode(GridGroupingExpandMode.RowClick)
            )

            .GroupPanel
            (p => p
            .EmptyPanelText("請把小組名稱拉到這裡")
            .Visible(false) // 讓使用者不能自己決定誰被Grouping起來
            .AllowColumnDragging(true)
            )

            //.GroupPanel(p => p.Visible(true))
            //.GroupPanel(p => p.AllowColumnDragging(false))
            //.GroupPanel(p => p.(false))
            //.Grouping(g => g.AutoExpandAll(true))


            .Editing(editing =>
            {
                //editing.Mode(GridEditMode.Batch);
                //editing.Mode(GridEditMode.Row);
                editing.Mode(GridEditMode.Cell);
                //editing.Mode(GridEditMode.Batch);
                //editing.Mode(GridEditMode.Form);
                //editing.Mode(GridEditMode.Popup);
                editing.AllowUpdating(true);
                //editing.AllowAdding(true);
                editing.AllowDeleting(true);
                editing.Texts(a => a.SaveAllChanges("上傳檔案").AddRow("新增").CancelAllChanges("取消變更").SaveRowChanges("儲存").CancelRowChanges("取消").EditRow("編輯").DeleteRow("刪除"));
            })

    .Columns(columns =>
    {
                @*columns.Add()
                    .DataField("Picture")
                    .Caption("照片")
                    .Width(60)
                    .AllowFiltering(false)
                    .AllowSorting(false)
                    .CellTemplate(@<text>
                    <div class="img-container">
                        <img src="<%= value %>" alt="" />
                    </div>
                    </text>        );*@


                @*columns.Add()
                    .Caption("測試")
                    //.DataField("Shepherd")
                    .Width(250)

                    .CellTemplate(@<text> @(Html.Partial("DropDownBox") )
                    </text>);*@

                @*columns.Add().DataField("SectionName").Caption("族系").GroupIndex(-1).MinWidth(10);*@
                    columns.Add().DataField("SmallGroupName").Caption("小組").GroupIndex(0).MinWidth(10);

                    columns.Add().DataField("FullName").Caption("姓名").AllowEditing(false).Fixed(true).Width(80).MinWidth(20).Alignment(HorizontalAlignment.Left).AllowEditing(false);
                    columns.Add().DataField("Status")
                    .Caption("會員身分").Alignment(HorizontalAlignment.Center)
                    .AllowEditing(true)
                    .Width(70).MinWidth(30).DataType(GridColumnDataType.Number)
                    .Lookup(lookup => lookup
                    .DataSource(new string[] { "牧師師母", "區長", "區牧", "領袖", "門徒", "家人", "未入組", "訪客", "外教會", "結案", })
                    //.DisplayExpr("Name")
                    //.ValueExpr("ID")
                    );

                    columns.Add().DataField("EquipmentStatus").Caption("裝備狀態").Alignment(HorizontalAlignment.Center).Fixed(false).Width(60).MinWidth(90).AllowEditing(false);
                    //columns.Add().DataField("SpiritualIdentity").Caption("受洗狀態").Alignment(HorizontalAlignment.Center).Fixed(false).Width(100).MinWidth(90).AllowEditing(true);
                    columns.Add().DataField("SpiritualIdentity")
            .Caption("受洗狀態").Alignment(HorizontalAlignment.Center)
            .AllowEditing(true)
            .Width(110).MinWidth(100).DataType(GridColumnDataType.String)
            .Lookup(lookup => lookup
            .DataSource(new string[] { "已受洗(本教會)", "已受洗(其他教會)", "-未知-", "未信主", "已決志", "未受洗" })
            //.DisplayExpr("Name")
            //.ValueExpr("ID")
            );
columns.Add().DataField("Phone").Caption("行動電話").Fixed(false).Width(120).MinWidth(90).Alignment(HorizontalAlignment.Left);
    //columns.Add().DataField("HomePhone").Caption("家裡電話").Fixed(false).Width(120).MinWidth(90).Alignment(HorizontalAlignment.Left);

    //columns.Add().DataField("BirthDate").Caption("生日").DataType(GridColumnDataType.Date).Format("yyyy/M/d").Alignment(HorizontalAlignment.Center).Fixed(false).Width(100).MinWidth(80);


    columns.Add().DataField("BirthDate").Caption("生日").DataType(GridColumnDataType.Date)
            .CalculateCellValue(@<text>
                                function(row)
                                {
                                    if( row.BirthDate != null )
                                    {
                                        //debugger;

                                        var ParsedDate = ParsingDate( row.BirthDate );
                                        if( ParsedDate.getFullYear() == 1901 )
                                        {
                                            return null;
                                        }
                                        else
                                        {
                                            return ParsedDate;
                                        }
                                    }
                                    else
                                    {return null;}
                                }
                            </text> )
                        .Format("yyyy/M/d").Alignment(HorizontalAlignment.Center).Fixed(false).Width(100).MinWidth(80);

        //columns.Add().DataField("Industry").Caption("職業及專長").Fixed(false).Width(100).MinWidth(90).Alignment(HorizontalAlignment.Left);
        columns.Add().DataField("Address").Caption("地址").Fixed(false).Width(200).MinWidth(90).Alignment(HorizontalAlignment.Left);
    })
                //.DataSource(d => d.WebApi().Controller("SmallGroupReport").Key("Id"))
                //.DataSource(Model.Members)
                .DataSource
                (d => d
                    .WebApi()
                    .Controller("Home")
                    .LoadAction("LoadMaintainPersonInfomation")
                    .InsertAction("InsertPresentRecord")
                    .DeleteAction("DeletePresentRecord")
                    .UpdateAction("UpdateMaintainPersonInfomationPresentRecord")
                    .Key("PresentRecordId")
                    .LoadParams(new { id = @ViewBag.ListId })
                )

                // 此處設定是讓選項偏左邊一排
                .OnContentReady(
                @<text>
                function(e)
                {
                moveEditColumnToLeft(e.component);
                //ShowToast( ReMindMessage , 'warning' , 20000)
                }
                </text>
                )


                //此處設定是鉛筆形狀還是"編輯"字樣
                .OnCellPrepared("cell_prepared")
    //.OnCellPrepared("cell_prepared_background_color")

    //.DataSourceOptions(d => d.Filter("['Id', '<=' , 6]"))
    )
        </div>
    }
    else
    {
        <div id="data-personal-grid">
            @(Html.DevExtreme().DataGrid()
            .ID("gridContainer")
            .ShowBorders(true)
            //.ColumnAutoWidth(true)
            //.Paging(p => p.PageSize(10))
            .Paging(paging => paging.Enabled(false))
            .AllowColumnResizing(true)
            .ColumnResizingMode(ColumnResizingMode.Widget)
            .ColumnFixing(c => c.Enabled(true))
            .LoadPanel(l => l.Text("更新資料中...").Enabled(false))
            .SearchPanel(s => s.Visible(false).Placeholder("搜尋.."))

            .Export(e => e.Enabled(false).AllowExportSelectedData(true))
            //.StateStoring(s => s
            //    .Enabled(true)
            //    .Type(StateStoringType.LocalStorage)
            //    .StorageKey("Id")
            //)

            //.OnRowUpdated("function () { alert('OnRowUpdated'); }")
            //.OnRowClick("function () { alert('OnRowClick'); }")
            //.OnSelectionChanged("function () { alert('OnSelectionChanged'); }" )
            //.OnCellClick("function () { alert('OnCellClick'); }")
            //.OnEditingStart("function () { alert('OnEditingStart'); }")
            //.OnEditorPrepared("function () { alert('OnEditorPrepared'); }")




            .ShowColumnLines(true)
            .ShowRowLines(true)
            .RowAlternationEnabled(true)
            .ShowBorders(true)

            .TwoWayBindingEnabled(true)


            .Grouping
            (g => g
            .ContextMenuEnabled(true)
            .ExpandMode(GridGroupingExpandMode.RowClick)
            )

            .GroupPanel
            (p => p
            .EmptyPanelText("請把小組名稱拉到這裡")
            .Visible(false) // 讓使用者不能自己決定誰被Grouping起來
            .AllowColumnDragging(true)
            )

            //.GroupPanel(p => p.Visible(true))
            //.GroupPanel(p => p.AllowColumnDragging(false))
            //.GroupPanel(p => p.(false))
            //.Grouping(g => g.AutoExpandAll(true))


            .Editing(editing =>
            {
                //editing.Mode(GridEditMode.Batch);
                //editing.Mode(GridEditMode.Row);
                editing.Mode(GridEditMode.Cell);
                //editing.Mode(GridEditMode.Batch);
                //editing.Mode(GridEditMode.Form);
                //editing.Mode(GridEditMode.Popup);
                editing.AllowUpdating(true);
                //editing.AllowAdding(true);
                //editing.AllowDeleting(true);
                editing.Texts(a => a.SaveAllChanges("上傳檔案").AddRow("新增").CancelAllChanges("取消變更").SaveRowChanges("儲存").CancelRowChanges("取消").EditRow("編輯").DeleteRow("刪除"));
            })

    .Columns(columns =>
    {
                @*columns.Add()
                    .DataField("Picture")
                    .Caption("照片")
                    .Width(60)
                    .AllowFiltering(false)
                    .AllowSorting(false)
                    .CellTemplate(@<text>
                    <div class="img-container">
                        <img src="<%= value %>" alt="" />
                    </div>
                    </text>        );*@


                @*columns.Add()
                    .Caption("測試")
                    //.DataField("Shepherd")
                    .Width(250)

                    .CellTemplate(@<text> @(Html.Partial("DropDownBox") )
                    </text>);*@

                @*columns.Add().DataField("SectionName").Caption("族系").GroupIndex(-1).MinWidth(10);*@
columns.Add().DataField("SmallGroupName").Caption("小組").GroupIndex(0).MinWidth(10);

columns.Add().DataField("FullName").Caption("姓名").AllowEditing(false).Fixed(true).Width(80).MinWidth(20).Alignment(HorizontalAlignment.Left).AllowEditing(false);
columns.Add().DataField("Status")
                    .Caption("會員身分").Alignment(HorizontalAlignment.Center)
                    .AllowEditing(true)
                    .Width(70).MinWidth(30).DataType(GridColumnDataType.Number)
                    .Lookup(lookup => lookup
                    .DataSource(new string[] { "牧師師母", "區長", "區牧", "領袖", "門徒", "家人", "未入組", "訪客", "外教會", "結案", })
                    //.DisplayExpr("Name")
                    //.ValueExpr("ID")
                    );

    columns.Add().DataField("EquipmentStatus").Caption("裝備狀態").Alignment(HorizontalAlignment.Center).Fixed(false).Width(60).MinWidth(90).AllowEditing(false);
    //columns.Add().DataField("SpiritualIdentity").Caption("受洗狀態").Alignment(HorizontalAlignment.Center).Fixed(false).Width(100).MinWidth(90).AllowEditing(true);
    columns.Add().DataField("SpiritualIdentity")
            .Caption("受洗狀態").Alignment(HorizontalAlignment.Center)
            .AllowEditing(true)
            .Width(110).MinWidth(100).DataType(GridColumnDataType.String)
            .Lookup(lookup => lookup
            .DataSource(new string[] { "已受洗(本教會)", "已受洗(其他教會)", "-未知-", "未信主", "已決志", "未受洗" })
            //.DisplayExpr("Name")
            //.ValueExpr("ID")
            );
    columns.Add().DataField("Phone").Caption("行動電話").Fixed(false).Width(120).MinWidth(90).Alignment(HorizontalAlignment.Left);
    //columns.Add().DataField("HomePhone").Caption("家裡電話").Fixed(false).Width(120).MinWidth(90).Alignment(HorizontalAlignment.Left);

    //columns.Add().DataField("BirthDate").Caption("生日").DataType(GridColumnDataType.Date).Format("yyyy/M/d").Alignment(HorizontalAlignment.Center).Fixed(false).Width(100).MinWidth(80);


    columns.Add().DataField("BirthDate").Caption("生日").DataType(GridColumnDataType.Date)
            .CalculateCellValue(@<text>
                                function(row)
                                {
                                    if( row.BirthDate != null )
                                    {
                                        //debugger;

                                        var ParsedDate = ParsingDate( row.BirthDate );
                                        if( ParsedDate.getFullYear() == 1901 )
                                        {
                                            return null;
                                        }
                                        else
                                        {
                                            return ParsedDate;
                                        }
                                    }
                                    else
                                    {return null;}
                                }
                            </text> )
                        .Format("yyyy/M/d").Alignment(HorizontalAlignment.Center).Fixed(false).Width(100).MinWidth(80);

        //columns.Add().DataField("Industry").Caption("職業及專長").Fixed(false).Width(100).MinWidth(90).Alignment(HorizontalAlignment.Left);
        columns.Add().DataField("Address").Caption("地址").Fixed(false).Width(200).MinWidth(90).Alignment(HorizontalAlignment.Left);
    })
                //.DataSource(d => d.WebApi().Controller("SmallGroupReport").Key("Id"))
                //.DataSource(Model.Members)
                .DataSource
                (d => d
                    .WebApi()
                    .Controller("Home")
                    .LoadAction("LoadMaintainPersonInfomation")
                    .InsertAction("InsertPresentRecord")
                    .DeleteAction("DeletePresentRecord")
                    .UpdateAction("UpdateMaintainPersonInfomationPresentRecord")
                    .Key("PresentRecordId")
                    .LoadParams(new { id = @ViewBag.ListId })
                )

                // 此處設定是讓選項偏左邊一排
                .OnContentReady(
                @<text>
                function(e)
                {
                moveEditColumnToLeft(e.component);
                //ShowToast( ReMindMessage , 'warning' , 20000)
                }
                </text>
                )


                //此處設定是鉛筆形狀還是"編輯"字樣
                .OnCellPrepared("cell_prepared")
                //.OnCellPrepared("cell_prepared_background_color")

                //.DataSourceOptions(d => d.Filter("['Id', '<=' , 6]"))
    )
        </div>
    }
</div>
<script>
    var markerUrl = "https://js.devexpress.com/Demos/RealtorApp/images/map-marker.png";
    //var markerUrl = "~/assets/images/map-marker.png";
    //var markerUrl = "~/wwwroot/assets/images/map-marker.png";
    var DataUploadStatus = "尚未上傳喔!";


    function ShowToast(LocalToastMessage, Type, DisplayTime) {
        var ToastInstance = $("#ToastReUpload").dxToast("instance");
        ToastInstance.option('message', LocalToastMessage);
        ToastInstance.option('type', Type);
        ToastInstance.option('displayTime', DisplayTime);
        ToastInstance.option('white-space', 'pre-line');
        ToastInstance.option('closeOnClick', true);
        ToastInstance.show();
    }
    function ParsingDate(input) {

        //debugger;
        if (typeof (input) === 'string') {
            //alert("ParseDate 001 : " + input);

            //debugger;

            var parts = input.split('-');

            var DaySplitedArray = parts[2].split('T');
            var ParsedDate = new Date(parts[0], parts[1] - 1, DaySplitedArray[0]);
            //var ParsedDate = new Date(parts[0], parts[1], DaySplitedArray[0]);
            //var ParsedDate = new Date(parts[0], parts[1], parts[2]);
            //var ParsedDate = new Date(parts[0], parts[1], DaySplitedArray[0]);

            //alert("ParseDate 002 : " + ParsedDate);

            return ParsedDate;
        }
        else {
            return getLocalDateFilter(input);
        }
    }
    function getLocalDateFilter(date) {

        //debugger;

        //alert("getODataLocalDateFilter !" + date);
        //alert("HELLO 我愛嘟嘟妞妞 006!" + date);

        //-- Description: For converting the date object to local time format
        //-- You can also convert this to UTC Date format
        //-- UTC Usage: getUTCMonth(), getUTCFullYear(), getUTCHours() ...

        var monthString;

        // 有的要加1 ，有的不加1，目前還沒時間去仔細探究原因
        //var rawMonth = (date.getMonth() + 1).toString();
        var rawMonth = (date.getMonth()).toString();
        if (rawMonth.length == 1) {
            monthString = "0" + rawMonth;
        }
        else { monthString = rawMonth; }

        var dateString;
        //var rawDate = date.getUTCDate().toString();
        var rawDate = date.getDate().toString();
        if (rawDate.length == 1) {
            dateString = "0" + rawDate;
        }
        else { dateString = rawDate; }


        var DateFilter = "";
        //DateFilter += date.getFullYear() + "-";
        //DateFilter += monthString + "-";
        //DateFilter += dateString;
        //DateFilter += " T" + date.getHours() + ":";
        //DateFilter += date.getMinutes() + ":";
        //DateFilter += date.getSeconds() + ":";
        //DateFilter += date.getMilliseconds();

        //return DateFilter;

        //return date.getFullYear() + "/" + rawMonth + "/" + rawDate;

        return new Date(date.getFullYear(), rawMonth, rawDate);
    }
    function onReady(e) {
    $.ajax({
        url: '@Url.Action("GetMarkers", "Home")',
        type: "POST",
        dataType: "json",
        success: function (data) {
            $("#map").dxMap("instance").option("markers", data);
        }
    });
    }


    function moveEditColumnToLeft(dataGrid) {
        dataGrid.columnOption("command:edit", {
            visibleIndex: -1,
            width: 50
        });
    }
    function cell_prepared(e) {
        if (e.rowType === "data" && e.column.command === "edit")
        {
                var isEditing = e.row.isEditing,
                $links = e.cellElement.find(".dx-link");

                $links.text("");

                if (isEditing) {
                    $links.filter(".dx-link-save").addClass("dx-icon-save");
                    $links.filter(".dx-link-cancel").addClass("dx-icon-revert");
                }
                else {
                    $links.filter(".dx-link-edit").addClass("dx-icon-edit");
                    $links.filter(".dx-link-delete").addClass("dx-icon-trash");
                }
        }

    }

</script>

<!--本地中文化-->
<script>
    var dictionary = {
        "zh-tw": {
            "dxScheduler-switcherAgenda": "會議內容", // 在這裡可以做最後的字典對應
            "Month": "月",
            "Subject": "主題",
            "Company": "Company",
            "Amount": "Amount",
            "PaymentDate": "Payment Date"
        },
    };
    DevExpress.localization.loadMessages(dictionary);
    var formatMessage = DevExpress.localization.formatMessage;

    var locale = getLocale();
    //DevExpress.localization.locale(navigator.language || navigator.browserLanguage);
    DevExpress.localization.locale(locale);

    function changeLocale(data) {
        setLocale(data.value);
        document.location.reload();
    }

    function getLocale() {
        //debugger;
        //sessionStorage.setItem("locale", 'zh');
        sessionStorage.setItem("locale", 'zh-tw');
        var locale = sessionStorage.getItem("locale");
        return locale != null ? locale : "tw";
    }
</script>
<!--本地中文化-->
<script src="~/js/devextreme/devextreme-intl.min.js"></script>
<script src="~/js/devextreme/localization/dx.messages.zh-tw.js"></script>

<script src="~/js/DataGridAjax.js"></script>
@*<script src="~/js/SelectDate.js"></script>*@
<script src="~/js/DropDownBox.js"></script>
<script src="~/js/DataGridAjax.js"></script>
<script src="~/js/LoadPanel.js"></script>

<link href="~/css/SmallGroupReportController.css" rel="stylesheet" />

