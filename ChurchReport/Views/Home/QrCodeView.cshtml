
<div class="long-title"><h3 id="classnamefield"></h3></div>
<div class="long-title"><h3 id="displaynamefield"></h3></div>

<script>
    function getSchedulerInstance() {
        return $("#scheduler").dxScheduler("instance");
    }
</script>

<script>
    var dictionary = {
        "zh-tw": {
            "dxScheduler-switcherAgenda": "會議內容", // 在這裡可以做最後的字典對應
            "Month": "月",
            "Subject": "主題",
            "Company": "Company",
            "Amount": "Amount",
            "PaymentDate": "Payment Date"
        },
    };
    DevExpress.localization.loadMessages(dictionary);
    var formatMessage = DevExpress.localization.formatMessage;

    var locale = getLocale();
    //DevExpress.localization.locale(navigator.language || navigator.browserLanguage);
    DevExpress.localization.locale(locale);

    function changeLocale(data) {
        setLocale(data.value);
        document.location.reload();
    }

    function getLocale() {
        //debugger;
        //sessionStorage.setItem("locale", 'zh');
        sessionStorage.setItem("locale", 'zh-tw');
        var locale = sessionStorage.getItem("locale");
        return locale != null ? locale : "tw";
    }

</script>

<script src="https://static.line-scdn.net/liff/edge/2.1/sdk.js"></script>

<script>


    window.onload = function (e) {
            //var message = '@TempData["Proponent"]';
            //window.alert( "耶和華" + message);
            liff.init(
                { liffId: '@TempData["Proponent"]' },
                function (data) {
                    //debugger;
                    //alert("測試 window.onload 001!");
                    initializeApp(data);
                    //alert("測試 window.onload 002!");
                }
            );
        };


        var UserId = "";
        var GroupId = "";
        var RoomId = "";
        var DisplayName = "";
        var ViewType = "";

        var ClassName = "";

    async function initializeApp(data) {
                //alert("測試 initializeApp-001 : " + data);
                //liff.scanCode();
           await liff.getProfile().then( function (profile) {
                DisplayName = profile.displayName;
                UserId = profile.userId;
                //alert("測試 UserId =" + UserId);
                GroupId= profile.aGroupId;
                RoomId = profile.aRoomId;
                ViewType = profile.aViewType;
                //alert("測試004!" + DisplayName);
                // 取得 UserId 的Ajax
                UpdateLineUserId(UserId, GroupId, RoomId, ViewType);

                document.getElementById('classnamefield').innerHTML = " 課程名稱 : " + '@TempData["ClassName"]';
                //document.getElementById('classnamefield').innerHTML = "";
                //document.getElementById('classnamefield').innerHTML = ClassName;

                document.getElementById('displaynamefield').innerHTML = profile.displayName + " : " + '@TempData["QrCodeId"]'+"的QR Code";
                //alert("測試005!" );
            });

        }


        function UpdateLineUserId(aUserLineId, aGroupId, aRoomId, aViewType ) {
                    // AJAX 區塊
                    return $.ajax
                    (
                        {
                            url: '@Url.Action("QrCodeGetLineId", "Home")',
                            data: { UserLineId: aUserLineId, GroupId: aGroupId, RoomId: aRoomId, ViewType: aViewType}, // 整理回傳給 Server 的點名資料
                            type: 'POST', //POST if you want to save, GET if you want to fetch data from server
                            //async: false,
                            success: function (data)
                            {
                                //alert("測試 AJAX-001 : " + data.message);
                                //ShowToast(data.message, "success", 2000);
                                //document.getElementById('classnamefield').innerHTML = " 課程名稱 : " + data.message;
                                //alert("測試 AJAX-002 : " + document.getElementById('classnamefield'));
                                //alert("測試 AJAX-003 : " + document.getElementById('classnamefield').value);
                                //alert("測試 AJAX-004 : " + document.getElementById('classnamefield').innerHTML);

                                //document.getElementById('classnamefield').value = " 課程名稱 : " + data.message;
                                //alert("測試 AJAX-005 : " + document.getElementById('classnamefield').innerHTML);
                                //alert("測試 AJAX-006 : " + document.getElementById('classnamefield').value);

                                document.getElementById('classnamefield').innerHTML = " 課程名稱 : " + data.message;
                                //alert("測試 AJAX-007 : " + document.getElementById('classnamefield').innerHTML);
                                //alert("測試 AJAX-008 : " + document.getElementById('classnamefield').value);

                                //ClassName = " 課程名稱 : " + data.message;
                                //alert("測試 AJAX-001!" );
                                //liff.scanCode();
                                ////getLoadPanelInstance().hide();
                                //var schedulerinstance = getSchedulerInstance();
                                ////alert("測試 AJAX-002!" + schedulerinstance);
                                //schedulerinstance.getDataSource().reload();
                                ////alert("測試 AJAX-003!" + schedulerinstance);
                                //schedulerinstance.repaint();
                                ////alert("測試 AJAX-004!" );
                                //window.setTimeout(UpdateLineUserId, 3000);
                                //window.location.href = "/Home/SchedulerView"
                            },

                            error: function (obj)
                            {
                                //getLoadPanelInstance().hide();
                                window.location.href = "/Home/Login";
                            }

                        }
                    );
                    // AJAX 區塊
        }

        function ShowToast(LocalToastMessage, Type, DisplayTime) {
            //debugger;
            var ToastInstance = $("#Toast").dxToast("instance");
            ToastInstance.option('message', LocalToastMessage);
            ToastInstance.option('type', Type);
            ToastInstance.option('displayTime', DisplayTime);
            ToastInstance.option('white-space', 'pre-line');
            ToastInstance.option('closeOnClick', true);
            ToastInstance.show();
        }

</script>

<link href="~/css/Scheduler.css" rel="stylesheet" />
<link href="~/css/Site.css" rel="stylesheet" />

<script src="~/js/devextreme/devextreme-intl.min.js"></script>
<script src="~/js/devextreme/localization/dx.messages.zh-tw.js"></script>
