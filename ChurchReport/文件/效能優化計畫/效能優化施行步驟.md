# ?? ChurchReport ASP.NET Core 效能優化施行步驟

> **版本**: 1.0  
> **目標框架**: .NET 10  
> **建立日期**: 2025年  
> **分支**: Jesus_QPay_4.9.9_SpeedUp

---

## ?? 目錄

1. [優化目標與 KPI](#優化目標與-kpi)
2. [第一階段：診斷與基準測量](#第一階段診斷與基準測量)
3. [第二階段：快取策略實作](#第二階段快取策略實作)
4. [第三階段：非同步與資料庫優化](#第三階段非同步與資料庫優化)
5. [第四階段：HTTP 與序列化優化](#第四階段http-與序列化優化)
6. [第五階段：記憶體與 GC 管理](#第五階段記憶體與-gc-管理)
7. [第六階段：.NET 10 特定優化](#第六階段net-10-特定優化)
8. [驗證與回歸測試](#驗證與回歸測試)
9. [檢查清單](#檢查清單)

---

## ?? 優化目標與 KPI

### 量化目標

| 指標 | 目前基準 | 目標值 | 改善幅度 |
|------|---------|--------|---------|
| 平均回應時間 (ART) | 待測量 | - | 降低 20% |
| 每秒請求數 (RPS) | 待測量 | - | 提高 30% |
| GC 暫停時間 | 待測量 | - | 降低 50% |
| 記憶體使用量 (Working Set) | 待測量 | - | 降低 20% |
| 首次載入時間 (Cold Start) | 待測量 | - | 降低 30% |

### 重點優化檔案清單

根據開啟的檔案分析，以下為高優先優化目標：

| 檔案 | 優化重點 | 優先級 |
|------|---------|--------|
| `DownloadIntegrateData.cs` | CRM 查詢快取、非同步 | ?? 高 |
| `DownloadListManager.cs` | 資料快取、減少重複查詢 | ?? 高 |
| `SmallGroupController.cs` | 非同步優化、Response Cache | ?? 高 |
| `ListManager.cs` | 記憶體管理、物件池 | ?? 中 |
| `WeeklyReportManager.cs` | 查詢優化、快取 | ?? 中 |
| `ToolUtilityClass.cs` | 連線池、非同步支援 | ?? 中 |
| `Startup.cs` | 中介軟體優化、壓縮 | ?? 低 |

---

## ?? 第一階段：診斷與基準測量

### 1.1 安裝診斷工具

```powershell
# 安裝 dotnet-counters
dotnet tool install --global dotnet-counters

# 安裝 dotnet-trace
dotnet tool install --global dotnet-trace

# 安裝 dotnet-dump
dotnet tool install --global dotnet-dump
```

### 1.2 建立效能基準

#### 步驟 1：啟動應用程式並取得 PID
```powershell
# 執行應用程式
dotnet run --project ChurchReport/ChurchReport.csproj

# 取得 Process ID
dotnet-counters ps
```

#### 步驟 2：監控系統計數器
```powershell
# 監控 GC、記憶體、執行緒池
dotnet-counters monitor --process-id <PID> --counters \
    System.Runtime[cpu-usage,working-set,gc-heap-size,gen-0-gc-count,gen-1-gc-count,gen-2-gc-count,threadpool-queue-length,threadpool-thread-count] \
    Microsoft.AspNetCore.Hosting[requests-per-second,total-requests,current-requests,failed-requests]
```

#### 步驟 3：記錄基準數據
建立檔案 `效能基準數據.json`：
```json
{
  "測量日期": "YYYY-MM-DD",
  "測量環境": "Development/Production",
  "基準數據": {
    "CPU使用率": "",
    "WorkingSet_MB": "",
    "GC_Heap_MB": "",
    "Gen0_GC_Count": "",
    "Gen1_GC_Count": "",
    "Gen2_GC_Count": "",
    "ThreadPool_QueueLength": "",
    "RPS": "",
    "平均回應時間_ms": ""
  }
}
```

### 1.3 識別慢查詢

#### 步驟 1：加入 EF Core 查詢記錄攔截器

在 `Startup.cs` 或 `Program.cs` 加入：
```csharp
// 建立查詢攔截器類別
public class SlowQueryInterceptor : DbCommandInterceptor
{
    private readonly ILogger<SlowQueryInterceptor> _logger;
    private const int SlowQueryThresholdMs = 100;

    public override DbDataReader ReaderExecuted(
        DbCommand command,
        CommandExecutedEventData eventData,
        DbDataReader result)
    {
        if (eventData.Duration.TotalMilliseconds > SlowQueryThresholdMs)
        {
            _logger.LogWarning(
                "慢查詢警告: {Duration}ms - {CommandText}",
                eventData.Duration.TotalMilliseconds,
                command.CommandText);
        }
        return base.ReaderExecuted(command, eventData, result);
    }
}
```

#### 步驟 2：手動記錄 CRM 查詢時間

在 `ToolUtilityClass.cs` 的關鍵方法加入計時：
```csharp
public Entity RetrieveEntity(string entityName, Guid id)
{
    var stopwatch = Stopwatch.StartNew();
    try
    {
        // 原有邏輯
        return result;
    }
    finally
    {
        stopwatch.Stop();
        if (stopwatch.ElapsedMilliseconds > 100)
        {
            System.Diagnostics.Debug.WriteLine(
                $"[CRM慢查詢] {entityName} - {stopwatch.ElapsedMilliseconds}ms");
        }
    }
}
```

### 1.4 API 端點效能分析

#### 重點監控端點：
| 端點 | 控制器 | 預期優化潛力 |
|------|--------|-------------|
| `/SmallGroup/IntegrateView` | SmallGroupController | 高 |
| `/SmallGroup/MultiGroupView` | SmallGroupController | 高 |
| `/api/SmallGroup/LoadIntegrate` | SmallGroupController | 高 |
| `/api/SmallGroup/GetChartDataList` | SmallGroupController | 中 |
| `/Home/Index` | HomeController | 低 |

---

## ??? 第二階段：快取策略實作

### 2.1 設定 Memory Cache

#### 步驟 1：在 `Startup.cs` 註冊服務
```csharp
public void ConfigureServices(IServiceCollection services)
{
    // 加入記憶體快取
    services.AddMemoryCache(options =>
    {
        options.SizeLimit = 1024; // 限制快取大小
    });
    
    // 加入分散式快取（選用）
    services.AddDistributedMemoryCache();
    
    // 或使用 Redis（生產環境建議）
    // services.AddStackExchangeRedisCache(options =>
    // {
    //     options.Configuration = Configuration.GetConnectionString("Redis");
    //     options.InstanceName = "ChurchReport_";
    // });
}
```

#### 步驟 2：建立快取服務類別

建立 `Services/CacheService.cs`：
```csharp
public interface ICacheService
{
    T GetOrCreate<T>(string key, Func<T> factory, TimeSpan? absoluteExpiration = null);
    Task<T> GetOrCreateAsync<T>(string key, Func<Task<T>> factory, TimeSpan? absoluteExpiration = null);
    void Remove(string key);
    void RemoveByPrefix(string prefix);
}

public class CacheService : ICacheService
{
    private readonly IMemoryCache _cache;
    private readonly ILogger<CacheService> _logger;
    private static readonly HashSet<string> _cacheKeys = new();
    private static readonly object _lock = new();

    public CacheService(IMemoryCache cache, ILogger<CacheService> logger)
    {
        _cache = cache;
        _logger = logger;
    }

    public T GetOrCreate<T>(string key, Func<T> factory, TimeSpan? absoluteExpiration = null)
    {
        return _cache.GetOrCreate(key, entry =>
        {
            entry.AbsoluteExpirationRelativeToNow = absoluteExpiration ?? TimeSpan.FromMinutes(10);
            entry.SlidingExpiration = TimeSpan.FromMinutes(5);
            entry.Size = 1;
            
            lock (_lock) { _cacheKeys.Add(key); }
            
            _logger.LogDebug("快取建立: {Key}", key);
            return factory();
        });
    }

    public async Task<T> GetOrCreateAsync<T>(string key, Func<Task<T>> factory, TimeSpan? absoluteExpiration = null)
    {
        return await _cache.GetOrCreateAsync(key, async entry =>
        {
            entry.AbsoluteExpirationRelativeToNow = absoluteExpiration ?? TimeSpan.FromMinutes(10);
            entry.SlidingExpiration = TimeSpan.FromMinutes(5);
            entry.Size = 1;
            
            lock (_lock) { _cacheKeys.Add(key); }
            
            _logger.LogDebug("快取建立: {Key}", key);
            return await factory();
        });
    }

    public void Remove(string key)
    {
        _cache.Remove(key);
        lock (_lock) { _cacheKeys.Remove(key); }
    }

    public void RemoveByPrefix(string prefix)
    {
        lock (_lock)
        {
            var keysToRemove = _cacheKeys.Where(k => k.StartsWith(prefix)).ToList();
            foreach (var key in keysToRemove)
            {
                _cache.Remove(key);
                _cacheKeys.Remove(key);
            }
        }
    }
}
```

### 2.2 快取策略定義

| 資料類型 | 快取 Key 格式 | 存活時間策略 | 預期效益 |
|---------|--------------|---------|---------|
| 小組清單 | `MultiGroupList_{Account}_{Date}` | 絕對 30 分鐘 | 減少 CRM 查詢 60% |
| 週報資料 | `WeeklyReport_{ListId}_{Date}` | 絕對 15 分鐘 | 減少查詢 40% |
| 圖表資料 | `ChartData_{ListId}` | 滑動 10 分鐘 | 加速頁面 50% |
| 會員清單 | `Members_{ListId}` | 絕對 10 分鐘 | 減少查詢 50% |
| 下拉選單 | `Dropdown_{Type}` | 絕對 60 分鐘 | 幾乎不需查詢 |

### 2.3 在 ListManager 實作快取

修改 `Models/ListManager.cs`：
```csharp
public class ListManager
{
    private readonly ICacheService _cacheService;
    
    // 注入快取服務
    public ListManager(ICacheService cacheService)
    {
        _cacheService = cacheService;
    }
    
    public void SetupListManager(string Account, string Password, DateTime aSelectDate, IOrganizationService organizationService = null)
    {
        var cacheKey = $"MultiGroupList_{Account}_{aSelectDate:yyyyMMdd}";
        
        var cachedData = _cacheService.GetOrCreate(cacheKey, () =>
        {
            // 原有的資料載入邏輯
            m_DownloadListManager.GetListManager(...);
            return new CachedListData
            {
                MultiGroupList = m_MultiGroupList,
                MultiGroupChartDataList = m_MultiGroupChartDataList,
                LoginType = LoginType,
                // ... 其他屬性
            };
        }, TimeSpan.FromMinutes(30));
        
        // 從快取還原資料
        m_MultiGroupList = cachedData.MultiGroupList;
        // ...
    }
}
```

### 2.4 Response Caching 設定

在 `SmallGroupController.cs` 加入：
```csharp
// 對靜態資料 API 加入快取
[HttpGet]
[ResponseCache(Duration = 300, VaryByQueryKeys = new[] { "id" })]
public object GetChartDataList(string WeeklyReportId, DataSourceLoadOptions loadOptions)
{
    // ...
}

// 或使用 OutputCache (.NET 7+)
[OutputCache(Duration = 300, VaryByQueryKeys = new[] { "id" })]
public object LoadIntegrate(string id, DataSourceLoadOptions loadOptions)
{
    // ...
}
```

---

## ? 第三階段：非同步與資料庫優化

### 3.1 識別同步阻塞點

#### 檢查清單：搜尋以下模式
```csharp
// ? 避免的模式
.Result
.Wait()
.GetAwaiter().GetResult()
Task.Run(() => ...).Result
lock (...)  // 考慮使用 SemaphoreSlim
```

#### 需檢查的檔案：
- [ ] `SmallGroupController.cs`
- [ ] `DownloadIntegrateData.cs`
- [ ] `DownloadListManager.cs`
- [ ] `ToolUtilityClass.cs`
- [ ] `WeeklyReportManager.cs`

### 3.2 Controller 非同步化

修改 `SmallGroupController.cs`：
```csharp
// Before ?
public IActionResult IntegrateView(string LoginParameter)
{
    SetupIntegrateViewData(LoginParameter);
    return View(...);
}

// After ?
public async Task<IActionResult> IntegrateView(
    string LoginParameter,
    CancellationToken cancellationToken = default)
{
    await SetupIntegrateViewDataAsync(LoginParameter, cancellationToken);
    return View(...);
}
```

### 3.3 CRM 查詢非同步化

在 `ToolUtilityClass.cs` 加入非同步方法：
```csharp
// 非同步版本
public async Task<Entity> RetrieveEntityAsync(
    string entityName, 
    Guid id,
    CancellationToken cancellationToken = default)
{
    return await Task.Run(() => 
        m_OrganizationService.Retrieve(entityName, id, new ColumnSet(true)),
        cancellationToken);
}

public async Task<EntityCollection> RetrieveMultipleAsync(
    QueryExpression query,
    CancellationToken cancellationToken = default)
{
    return await Task.Run(() => 
        m_OrganizationService.RetrieveMultiple(query),
        cancellationToken);
}
```

### 3.4 批次查詢優化

修改 `DownloadIntegrateData.cs`：
```csharp
// Before ? - N+1 查詢問題
foreach (Entity PresentRecordEntity in PresentRecordCollection.Entities)
{
    Entity aContactEntity = m_ToolUtilityClass.RetrieveEntity("contact", id);
    // ...
}

// After ? - 批次查詢
private async Task<Dictionary<Guid, Entity>> BatchRetrieveContactsAsync(
    IEnumerable<Guid> contactIds,
    CancellationToken cancellationToken)
{
    var query = new QueryExpression("contact")
    {
        ColumnSet = new ColumnSet(true),
        Criteria = new FilterExpression
        {
            Conditions =
            {
                new ConditionExpression("contactid", ConditionOperator.In, contactIds.ToArray())
            }
        }
    };
    
    var results = await m_ToolUtilityClass.RetrieveMultipleAsync(query, cancellationToken);
    return results.Entities.ToDictionary(e => e.Id);
}
```

### 3.5 連線池最佳化

檢查 `CrmConnectionPool` 設定：
```csharp
public class CrmConnectionPoolOptions
{
    public int MaxPoolSize { get; set; } = 100;
    public int MinPoolSize { get; set; } = 10;
    public TimeSpan ConnectionTimeout { get; set; } = TimeSpan.FromSeconds(30);
    public TimeSpan IdleTimeout { get; set; } = TimeSpan.FromMinutes(5);
}
```

---

## ?? 第四階段：HTTP 與序列化優化

### 4.1 啟用 Response Compression

修改 `Startup.cs`：
```csharp
public void ConfigureServices(IServiceCollection services)
{
    // 加入壓縮服務
    services.AddResponseCompression(options =>
    {
        options.EnableForHttps = true;
        options.Providers.Add<BrotliCompressionProvider>();
        options.Providers.Add<GzipCompressionProvider>();
        options.MimeTypes = ResponseCompressionDefaults.MimeTypes.Concat(
            new[] { "application/json", "text/html", "application/javascript" });
    });
    
    services.Configure<BrotliCompressionProviderOptions>(options =>
    {
        options.Level = CompressionLevel.Optimal;
    });
    
    services.Configure<GzipCompressionProviderOptions>(options =>
    {
        options.Level = CompressionLevel.Optimal;
    });
}

public void Configure(IApplicationBuilder app)
{
    // 在管線最前面加入
    app.UseResponseCompression();
    
    // ... 其他中介軟體
}
```

### 4.2 JSON 序列化優化 (Source Generator)

#### 步驟 1：建立 JSON Context

建立 `JsonContexts/AppJsonContext.cs`：
```csharp
using System.Text.Json.Serialization;
using ChurchReport.Models;

namespace ChurchReport.JsonContexts;

[JsonSerializable(typeof(ListSmallGroupWeeklyReport))]
[JsonSerializable(typeof(List<Member>))]
[JsonSerializable(typeof(List<WeeklyReportRecord>))]
[JsonSerializable(typeof(List<ChartData>))]
[JsonSerializable(typeof(MultiGroupList))]
[JsonSerializable(typeof(SmallGroupDataList))]
[JsonSourceGenerationOptions(
    PropertyNamingPolicy = JsonKnownNamingPolicy.CamelCase,
    WriteIndented = false,
    DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull)]
public partial class AppJsonContext : JsonSerializerContext
{
}
```

#### 步驟 2：在 Startup 配置

```csharp
services.AddControllers()
    .AddJsonOptions(options =>
    {
        options.JsonSerializerOptions.PropertyNamingPolicy = JsonNamingPolicy.CamelCase;
        options.JsonSerializerOptions.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;
        options.JsonSerializerOptions.TypeInfoResolver = AppJsonContext.Default;
    });
```

### 4.3 HTTP/2 設定

在 `Program.cs` 或 `appsettings.json`：
```json
{
  "Kestrel": {
    "EndpointDefaults": {
      "Protocols": "Http1AndHttp2AndHttp3"
    },
    "Limits": {
      "MaxConcurrentConnections": 100,
      "MaxConcurrentUpgradedConnections": 100,
      "MaxRequestBodySize": 30000000,
      "MinRequestBodyDataRate": {
        "BytesPerSecond": 100,
        "GracePeriod": "00:00:10"
      }
    }
  }
}
```

### 4.4 靜態檔案優化

修改 `Startup.cs`：
```csharp
// .NET 10 使用 MapStaticAssets
app.MapStaticAssets();

// 或傳統方式加入快取
app.UseStaticFiles(new StaticFileOptions
{
    OnPrepareResponse = ctx =>
    {
        // 快取靜態檔案 1 年
        ctx.Context.Response.Headers.Append(
            "Cache-Control", "public,max-age=31536000");
    }
});
```

---

## ?? 第五階段：記憶體與 GC 管理

### 5.1 啟用 Server GC

修改 `ChurchReport.csproj`：
```xml
<PropertyGroup>
  <ServerGarbageCollection>true</ServerGarbageCollection>
  <ConcurrentGarbageCollection>true</ConcurrentGarbageCollection>
  <RetainVMGarbageCollection>true</RetainVMGarbageCollection>
</PropertyGroup>
```

### 5.2 Object Pool 實作

建立 `Services/ObjectPoolService.cs`：
```csharp
public class MemberPoolPolicy : IPooledObjectPolicy<Member>
{
    public Member Create() => new Member();
    
    public bool Return(Member obj)
    {
        // 重置物件狀態
        obj.PresentRecordId = null;
        obj.FullName = null;
        obj.Phone = null;
        // ... 重置其他屬性
        return true;
    }
}

// 在 Startup.cs 註冊
services.AddSingleton<ObjectPool<Member>>(sp =>
{
    var policy = new MemberPoolPolicy();
    return new DefaultObjectPool<Member>(policy, 100);
});
```

### 5.3 減少 LINQ 分配

```csharp
// Before ? - 產生中間集合
var result = members
    .Where(m => m.Status != "結案")
    .Select(m => m.FullName)
    .ToList();

// After ? - 使用 Span/Memory 或避免中間集合
var result = new List<string>(members.Count);
foreach (var m in members)
{
    if (m.Status != "結案")
        result.Add(m.FullName);
}

// 或使用 ArrayPool
var pool = ArrayPool<string>.Shared;
var buffer = pool.Rent(members.Count);
try
{
    // 使用 buffer
}
finally
{
    pool.Return(buffer);
}
```

### 5.4 字串處理優化

```csharp
// Before ? - 字串串接
string report = "";
foreach (var item in items)
{
    report += item.Name + Environment.NewLine;
}

// After ? - StringBuilder
var sb = new StringBuilder(items.Count * 50); // 預估容量
foreach (var item in items)
{
    sb.Append(item.Name).AppendLine();
}
string report = sb.ToString();
```

---

## ?? 第六階段：.NET 10 特定優化

### 6.1 HybridCache (.NET 9+)

```csharp
// 在 Startup.cs
services.AddHybridCache(options =>
{
    options.MaximumPayloadBytes = 1024 * 1024; // 1MB
    options.MaximumKeyLength = 512;
    options.DefaultEntryOptions = new HybridCacheEntryOptions
    {
        Expiration = TimeSpan.FromMinutes(10),
        LocalCacheExpiration = TimeSpan.FromMinutes(5)
    };
]);

// 使用方式
public class MyService
{
    private readonly HybridCache _cache;
    
    public async Task<Data> GetDataAsync(string key)
    {
        return await _cache.GetOrCreateAsync(
            key,
            async cancel => await LoadDataAsync(cancel),
            cancellationToken: default);
    }
}
```

### 6.2 MapStaticAssets

```csharp
// Program.cs (.NET 10)
var app = builder.Build();

// 取代 UseStaticFiles
app.MapStaticAssets();
```

### 6.3 IAsyncEnumerable 串流

```csharp
// 大量資料串流回傳
[HttpGet]
public async IAsyncEnumerable<Member> GetMembersStream(
    [EnumeratorCancellation] CancellationToken cancellationToken)
{
    await foreach (var member in GetMembersAsync(cancellationToken))
    {
        yield return member;
    }
}
```

---

## ? 驗證與回歸測試

### 效能測試腳本

建立 `Tests/PerformanceTests.cs`：
```csharp
[MemoryDiagnoser]
[SimpleJob(RuntimeMoniker.Net100)]
public class ApiPerformanceBenchmark
{
    private HttpClient _client;
    
    [GlobalSetup]
    public void Setup()
    {
        _client = new HttpClient
        {
            BaseAddress = new Uri("https://localhost:5001")
        };
    }
    
    [Benchmark]
    public async Task<string> IntegrateView_Benchmark()
    {
        var response = await _client.GetAsync("/SmallGroup/IntegrateView/AccountPassword");
        return await response.Content.ReadAsStringAsync();
    }
    
    [Benchmark]
    public async Task<string> LoadIntegrate_Benchmark()
    {
        var response = await _client.GetAsync("/api/SmallGroup/LoadIntegrate?id=test");
        return await response.Content.ReadAsStringAsync();
    }
}
```

### 執行效能測試
```powershell
# 執行 BenchmarkDotNet
dotnet run -c Release --project Tests/PerformanceTests.csproj
```

---

## ?? 檢查清單

### 第一階段完成條件
- [ ] 安裝 dotnet-counters
- [ ] 記錄基準效能數據
- [ ] 識別 Top 5 慢查詢
- [ ] 識別 Top 5 慢 API

### 第二階段完成條件
- [x] 註冊 MemoryCache 服務（已在 Startup.cs 配置）
- [x] 建立 CacheService 類別（ChurchReport/Services/Caching/）
  - [x] ICacheService.cs - 快取服務介面
  - [x] CacheService.cs - 快取服務實作
  - [x] CacheKeys.cs - 快取鍵常數與建構器
- [x] 實作 ListManager 快取
  - [x] ListManagerCacheExtensions.cs - ListManager 快取擴充方法
  - [x] CachedListManagerData - 快取資料結構
- [x] 加入 ResponseCache 屬性
  - [x] GetChartDataList - 快取 5 分鐘
  - [x] GetMultiGroupChartDataList - 快取 5 分鐘
  - [x] AssignSmallGroupGet - 快取 1 分鐘

### 第三階段完成條件
- [x] 移除所有 .Result/.Wait()（SmallGroupController 已使用 async/await）
- [x] Controller 方法改為 async（MultiGroupView, IntegrateView, UpdateSmallGroupPresentRecord 等已完成）
- [x] 建立 CRM 非同步查詢擴充方法
  - [x] CrmAsyncExtensions.cs - 非同步 CRM 操作擴充
  - [x] BatchRetrieveAsync - 批次查詢解決 N+1 問題
  - [x] RetrieveAllAsync - 自動分頁查詢
  - [x] ParallelRetrieveAsync - 並行查詢
- [x] 檢查連線池設定
  - [x] MinPoolSize: 3（已配置）
  - [x] MaxPoolSize: 20（已配置）
  - [x] ConnectionTimeoutSeconds: 30（已配置）
  - [x] IdleTimeoutMinutes: 10（已配置）
- [ ] 實作批次 CRM 查詢（待整合到 DownloadIntegrateData）

### 第四階段完成條件
- [x] 啟用 Response Compression
  - [x] 註冊 Brotli 和 Gzip 壓縮服務
  - [x] 設定壓縮等級為 Optimal
  - [x] 加入 JSON、HTML、JavaScript、CSS、SVG MIME 類型
- [ ] 建立 JSON Source Generator（本專案使用 Newtonsoft.Json，暫不適用）
- [ ] 確認 HTTP/2 啟用（需部署環境支援）
- [x] 設定靜態檔案快取
  - [x] 靜態檔案快取 1 年 (max-age=31536000)
  - [x] 使用 Cache-Control: public 標頭

### 第五階段完成條件
- [x] 啟用 Server GC
  - [x] ServerGarbageCollection: true
  - [x] ConcurrentGarbageCollection: true
  - [x] RetainVMGarbageCollection: true
  - [x] PublishReadyToRun: true（加速冷啟動）
- [x] 實作 Object Pool
  - [x] StringBuilderPool.cs - StringBuilder 物件池
  - [x] IStringBuilderPool 介面
  - [x] 在 Startup.cs 註冊服務
- [x] 優化 LINQ 查詢
  - [x] ArrayPoolExtensions.cs - ArrayPool 擴充方法
  - [x] WhereToList - 預先配置容量的篩選
  - [x] SelectToList - 預先配置容量的轉換
  - [x] ToListWithCapacity - 預估容量的 ToList
- [ ] 優化字串處理（已提供工具，待整合到現有程式碼）

### 第六階段完成條件
- [ ] 評估 HybridCache 使用
- [ ] 升級至 MapStaticAssets
- [ ] 評估 IAsyncEnumerable 使用

### 最終驗證
- [ ] 效能回歸測試通過
- [ ] ART 降低 ? 20%
- [ ] RPS 提高 ? 30%
- [ ] 無記憶體洩漏
- [ ] 所有功能正常運作

---

## ?? 參考資源

- [ASP.NET Core 效能最佳做法](https://learn.microsoft.com/aspnet/core/performance/performance-best-practices)
- [.NET 效能](https://learn.microsoft.com/dotnet/core/performance/)
- [記憶體管理](https://learn.microsoft.com/dotnet/standard/garbage-collection/)
- [快取指引](https://learn.microsoft.com/aspnet/core/performance/caching/overview)

---

> **下次更新時間**: 完成第一階段後  
> **維護者**: 開發團隊
