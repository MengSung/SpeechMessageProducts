# 全程式效能剖析子系統 — 實作計畫

> **For agentic workers:** 建議用 superpowers:subagent-driven-development（或 superpowers:executing-plans）逐任務實作。步驟用 `- [ ]` 追蹤。

**Goal:** 為整個程式（啟動 + 每個 web 請求）加入集中式、Debug-only 的計時，於 `Logs\Trace.log` 輸出，據以找出並修正效能瓶頸；Release 完全無痕；絕不引入 Session-Leakage。

**Architecture:** 每請求計時狀態只存在 `HttpContext.Items`（per-request、結束即回收）。以「全域 Action Filter（量 action）+ `IOrganizationService` 計時裝飾器（數 CRM 呼叫）+ 獨立中間件（輸出 `[Perf]`）+ `IToolUtilityProvider` 裝飾器（涵蓋靜態 CRM 路徑）+ `StartupProfiler`（量啟動）」組成。全部 `#if DEBUG`，Release 不存在。

**Tech Stack:** ASP.NET Core MVC (net10.0)、Microsoft.Xrm.Sdk（Dataverse/CRM）、`System.Diagnostics.Debug`/`Stopwatch`。

**對應規格：** `文件\效能優化計畫\2026-06-17_全程式效能剖析子系統_設計spec.md`

---

## 重要前提（請先讀）

> **⚠️ 本計畫含文末「v2 修訂」**（2026-06-17 對抗式檢視後）：覆寫 Task 1/2/4/6/7/8/9，並新增 `ProfilingSwitch`（Task 0）與分析腳本（Task 11）。**實作前先讀文末 v2 修訂，並以該處程式碼為準。**

1. **本方案不使用 TDD/單元測試框架**：此 solution 無任何測試專案（已確認 `Tests/**/*.csproj` 不存在），且程式庫慣例本就無單元測試。強行新增測試專案屬未經要求的結構性變更。**驗證方式沿用本期已成功且使用者信任的方法**：①`dotnet build -c Debug`（驗證可編譯）②`dotnet build -c Release` + 掃描 `bin\Release\...\ChurchReport.dll` 確認 `[Perf` 字串不存在（Release 無痕）③Debug 模式實跑 + 讀 `Trace.log` 驗證輸出。
2. **全部 `#if DEBUG`**：每個新類別整檔包在 `#if DEBUG ... #endif`，所有引用/註冊也包 `#if DEBUG`。Release 組件中這些型別與字串完全不存在。
3. **Session-Leakage 鐵則**：計時狀態只放 `HttpContext.Items`；只記「數字 + entity 邏輯名 + 操作名」，**不得**記錄 ID/欄位值/使用者身分；不得用 static/singleton 可變狀態保存請求計時資料。
4. **路徑慣例**：原始碼在 `ChurchReport\ChurchReport\`（非本 worktree）。以下相對路徑均相對該專案根。
5. **每任務結尾 commit**；若目前在預設分支，先開分支。

---

## File Structure

| 檔案 | 責任 |
|------|------|
| `Diagnostics/Profiling/PerfThresholds.cs`（新）| 門檻常數（N+1、慢呼叫） |
| `Diagnostics/Profiling/RequestProfiler.cs`（新）| 每請求計時容器 + 輸出行建構 |
| `Diagnostics/Profiling/StartupProfiler.cs`（新）| 啟動階段計時（`using` 區塊） |
| `Diagnostics/Profiling/TimedOrganizationService.cs`（新）| `IOrganizationService` 計時裝飾器 |
| `Diagnostics/Profiling/TimedToolUtilityProvider.cs`（新）| `IToolUtilityProvider` 裝飾器（涵蓋靜態 CRM 路徑） |
| `Filters/PerfTimingActionFilter.cs`（新）| 量每支 action 執行時間 |
| `Middleware/PerfProfilingMiddleware.cs`（新）| 建立 profiler、請求結束輸出 `[Perf]` |
| `Controllers/BaseChurchController.cs`（改）| `GetConnection` 包裝、`ReleaseConnection` 解包（`#if DEBUG`） |
| `Startup.cs`（改）| 註冊 Filter/中間件/Provider 裝飾器、`StartupProfiler` 包覆啟動階段（皆 `#if DEBUG`） |

---

## Task 1: 門檻常數 `PerfThresholds`

**Files:** Create `Diagnostics/Profiling/PerfThresholds.cs`

- [ ] **Step 1: 建立檔案（完整內容）**

```csharp
#if DEBUG
namespace ChurchReport.Diagnostics.Profiling
{
    /// <summary>效能剖析門檻（僅 Debug）。集中管理，方便調整。</summary>
    internal static class PerfThresholds
    {
        /// <summary>同一請求 CRM 呼叫次數超過此值 → 視為疑似 N+1，加印明細。</summary>
        public const int NPlusOneCrmCount = 10;

        /// <summary>單次 CRM 呼叫超過此毫秒 → 視為慢呼叫，加印明細。</summary>
        public const long SlowSingleCallMs = 100;
    }
}
#endif
```

- [ ] **Step 2: 編譯驗證**

Run: `dotnet build "ChurchReport\ChurchReport.csproj" -c Debug --nologo -v minimal`
Expected: 建置成功，0 錯誤。

- [ ] **Step 3: Commit**

```bash
git add ChurchReport/Diagnostics/Profiling/PerfThresholds.cs
git commit -m "perf(profiling): add PerfThresholds constants (Debug-only)"
```

---

## Task 2: 每請求容器 `RequestProfiler`

**Files:** Create `Diagnostics/Profiling/RequestProfiler.cs`

- [ ] **Step 1: 建立檔案（完整內容）**

```csharp
#if DEBUG
using System.Collections.Generic;
using System.Diagnostics;
using System.Text;

namespace ChurchReport.Diagnostics.Profiling
{
    /// <summary>
    /// 單一請求的計時容器。只放於 HttpContext.Items，請求結束即回收。
    /// 只記錄：數字 + entity 邏輯名 + 操作名。不含任何記錄 ID/欄位值/使用者身分。
    /// </summary>
    public sealed class RequestProfiler
    {
        public const string ItemsKey = "__PerfProfiler";

        private readonly Stopwatch _total = Stopwatch.StartNew();
        private readonly object _lock = new object();
        private long _actionMs;
        private int _crmCount;
        private long _crmTotalMs;
        private string _slowestEntity;
        private string _slowestOp;
        private long _slowestMs;
        private readonly Dictionary<string, (int count, long ms)> _crmByOp =
            new Dictionary<string, (int, long)>();
        private readonly List<(string entity, string op, long ms)> _slowCalls =
            new List<(string, string, long)>();

        public int CrmCount => _crmCount;

        public void SetActionElapsed(long ms) => _actionMs = ms;

        public void RecordCrmCall(string entity, string op, long ms)
        {
            entity ??= "?";
            op ??= "?";
            lock (_lock)
            {
                _crmCount++;
                _crmTotalMs += ms;
                if (ms > _slowestMs) { _slowestMs = ms; _slowestEntity = entity; _slowestOp = op; }

                var key = entity + "." + op;
                if (_crmByOp.TryGetValue(key, out var cur))
                    _crmByOp[key] = (cur.count + 1, cur.ms + ms);
                else
                    _crmByOp[key] = (1, ms);

                if (ms > PerfThresholds.SlowSingleCallMs)
                    _slowCalls.Add((entity, op, ms));
            }
        }

        public long StopAndGetTotalMs()
        {
            _total.Stop();
            return _total.ElapsedMilliseconds;
        }

        public string BuildSummaryLine(string path, long totalMs)
        {
            var slowest = _slowestMs > 0 ? $" slowest={_slowestEntity}.{_slowestOp}:{_slowestMs}ms" : "";
            return $"[Perf] path={path} total={totalMs}ms action={_actionMs}ms "
                 + $"crm{{n={_crmCount},ms={_crmTotalMs}}}{slowest}";
        }

        public IEnumerable<string> BuildEscalationLines(string path)
        {
            var lines = new List<string>();
            if (_crmCount > PerfThresholds.NPlusOneCrmCount)
            {
                var sb = new StringBuilder();
                lock (_lock)
                {
                    foreach (var kv in _crmByOp)
                        sb.Append($"{kv.Key} ×{kv.Value.count} (Σ{kv.Value.ms}ms), ");
                }
                lines.Add($"[Perf-N+1] path={path} crm.n={_crmCount} 詳列: "
                        + sb.ToString().TrimEnd(',', ' '));
            }
            lock (_lock)
            {
                foreach (var c in _slowCalls)
                    lines.Add($"[Perf-Slow] path={path} {c.entity}.{c.op} {c.ms}ms");
            }
            return lines;
        }
    }
}
#endif
```

- [ ] **Step 2: 編譯驗證** — `dotnet build "ChurchReport\ChurchReport.csproj" -c Debug --nologo -v minimal` → 成功。
- [ ] **Step 3: Commit** — `git add ... && git commit -m "perf(profiling): add per-request RequestProfiler (Debug-only, HttpContext.Items)"`

---

## Task 3: 啟動計時 `StartupProfiler`

**Files:** Create `Diagnostics/Profiling/StartupProfiler.cs`

- [ ] **Step 1: 建立檔案（完整內容）**

```csharp
#if DEBUG
using System;
using System.Diagnostics;

namespace ChurchReport.Diagnostics.Profiling
{
    /// <summary>
    /// 啟動階段計時。用法：using (StartupProfiler.Phase("ConfigureServices")) { ... }
    /// 無共用可變狀態（每個 Phase 只持有自己的 label+Stopwatch）。啟動時無任何使用者/請求，零 Session-Leakage 風險。
    /// </summary>
    public static class StartupProfiler
    {
        public static IDisposable Phase(string name) => new PhaseTimer(name);

        private sealed class PhaseTimer : IDisposable
        {
            private readonly string _name;
            private readonly Stopwatch _sw;
            public PhaseTimer(string name) { _name = name; _sw = Stopwatch.StartNew(); }
            public void Dispose()
            {
                _sw.Stop();
                Debug.WriteLine($"[Perf-Startup] phase={_name} ms={_sw.ElapsedMilliseconds}");
            }
        }
    }
}
#endif
```

- [ ] **Step 2: 編譯驗證** — Debug build 成功。
- [ ] **Step 3: Commit** — `git commit -m "perf(profiling): add StartupProfiler (Debug-only)"`

---

## Task 4: CRM 計時裝飾器 `TimedOrganizationService`

**Files:** Create `Diagnostics/Profiling/TimedOrganizationService.cs`

> 它包住真正的 `IOrganizationService`，每次呼叫計時並寫入「當前請求的 RequestProfiler」（由 `IHttpContextAccessor` 解析；無請求時略過）。本身不保存任何請求資料。

- [ ] **Step 1: 建立檔案（完整內容）**

```csharp
#if DEBUG
using System;
using System.Diagnostics;
using Microsoft.AspNetCore.Http;
using Microsoft.Xrm.Sdk;
using Microsoft.Xrm.Sdk.Query;

namespace ChurchReport.Diagnostics.Profiling
{
    /// <summary>IOrganizationService 計時裝飾器（僅 Debug）。記錄 entity/操作/毫秒到當前請求 profiler。</summary>
    public sealed class TimedOrganizationService : IOrganizationService
    {
        private readonly IOrganizationService _inner;
        private readonly IHttpContextAccessor _http;

        public TimedOrganizationService(IOrganizationService inner, IHttpContextAccessor http)
        {
            _inner = inner;
            _http = http;
        }

        /// <summary>供 ReleaseConnection 解包，歸還真正連線給連線池。</summary>
        public IOrganizationService Inner => _inner;

        private RequestProfiler Profiler =>
            _http?.HttpContext?.Items != null
            && _http.HttpContext.Items.TryGetValue(RequestProfiler.ItemsKey, out var p)
                ? p as RequestProfiler
                : null;

        private T Time<T>(string entity, string op, Func<T> call)
        {
            var sw = Stopwatch.StartNew();
            try { return call(); }
            finally { sw.Stop(); Profiler?.RecordCrmCall(entity, op, sw.ElapsedMilliseconds); }
        }

        private void Time(string entity, string op, Action call)
        {
            var sw = Stopwatch.StartNew();
            try { call(); }
            finally { sw.Stop(); Profiler?.RecordCrmCall(entity, op, sw.ElapsedMilliseconds); }
        }

        private static string QueryEntity(QueryBase q) =>
            q is QueryExpression qe ? qe.EntityName :
            q is QueryByAttribute qa ? qa.EntityName : "?";

        public Guid Create(Entity entity) =>
            Time(entity?.LogicalName, "Create", () => _inner.Create(entity));

        public Entity Retrieve(string entityName, Guid id, ColumnSet columnSet) =>
            Time(entityName, "Retrieve", () => _inner.Retrieve(entityName, id, columnSet));

        public EntityCollection RetrieveMultiple(QueryBase query) =>
            Time(QueryEntity(query), "RetrieveMultiple", () => _inner.RetrieveMultiple(query));

        public void Update(Entity entity) =>
            Time(entity?.LogicalName, "Update", () => _inner.Update(entity));

        public void Delete(string entityName, Guid id) =>
            Time(entityName, "Delete", () => _inner.Delete(entityName, id));

        public OrganizationResponse Execute(OrganizationRequest request) =>
            Time(request?.RequestName ?? "Execute", "Execute", () => _inner.Execute(request));

        public void Associate(string entityName, Guid entityId, Relationship relationship,
            EntityReferenceCollection relatedEntities) =>
            Time(entityName, "Associate", () => _inner.Associate(entityName, entityId, relationship, relatedEntities));

        public void Disassociate(string entityName, Guid entityId, Relationship relationship,
            EntityReferenceCollection relatedEntities) =>
            Time(entityName, "Disassociate", () => _inner.Disassociate(entityName, entityId, relationship, relatedEntities));
    }
}
#endif
```

- [ ] **Step 2: 編譯驗證** — Debug build 成功（確認 `Microsoft.Xrm.Sdk` 介面簽章吻合）。
- [ ] **Step 3: Commit** — `git commit -m "perf(profiling): add TimedOrganizationService CRM decorator (Debug-only)"`

---

## Task 5: Action 計時 Filter `PerfTimingActionFilter`

**Files:** Create `Filters/PerfTimingActionFilter.cs`

- [ ] **Step 1: 建立檔案（完整內容）**

```csharp
#if DEBUG
using System.Diagnostics;
using ChurchReport.Diagnostics.Profiling;
using Microsoft.AspNetCore.Mvc.Filters;

namespace ChurchReport.Filters
{
    /// <summary>量每支 controller action 執行時間，寫入當前請求 profiler（僅 Debug）。</summary>
    public sealed class PerfTimingActionFilter : IActionFilter
    {
        private const string Key = "__PerfActionStopwatch";

        public void OnActionExecuting(ActionExecutingContext context)
        {
            context.HttpContext.Items[Key] = Stopwatch.StartNew();
        }

        public void OnActionExecuted(ActionExecutedContext context)
        {
            if (context.HttpContext.Items.TryGetValue(Key, out var o) && o is Stopwatch sw)
            {
                sw.Stop();
                if (context.HttpContext.Items.TryGetValue(RequestProfiler.ItemsKey, out var p)
                    && p is RequestProfiler profiler)
                {
                    profiler.SetActionElapsed(sw.ElapsedMilliseconds);
                }
            }
        }
    }
}
#endif
```

- [ ] **Step 2: 編譯驗證** — Debug build 成功。
- [ ] **Step 3: Commit** — `git commit -m "perf(profiling): add PerfTimingActionFilter (Debug-only)"`

---

## Task 6: 輸出中間件 `PerfProfilingMiddleware`

**Files:** Create `Middleware/PerfProfilingMiddleware.cs`

> 請求開始建 `RequestProfiler` 入 `Items`；結束輸出 `[Perf]`（+ 視門檻加印明細）。**不修改既有 `PerformanceMonitoringMiddleware`**。

- [ ] **Step 1: 建立檔案（完整內容）**

```csharp
#if DEBUG
using System.Diagnostics;
using System.Threading.Tasks;
using ChurchReport.Diagnostics.Profiling;
using Microsoft.AspNetCore.Http;

namespace ChurchReport.Middleware
{
    /// <summary>建立每請求 profiler 並於結束輸出 [Perf]（僅 Debug）。</summary>
    public sealed class PerfProfilingMiddleware
    {
        private readonly RequestDelegate _next;
        public PerfProfilingMiddleware(RequestDelegate next) => _next = next;

        public async Task Invoke(HttpContext context)
        {
            var profiler = new RequestProfiler();
            context.Items[RequestProfiler.ItemsKey] = profiler;
            try
            {
                await _next(context);
            }
            finally
            {
                var totalMs = profiler.StopAndGetTotalMs();
                var path = context.Request?.Path.Value ?? "?";
                Debug.WriteLine(profiler.BuildSummaryLine(path, totalMs));
                foreach (var line in profiler.BuildEscalationLines(path))
                    Debug.WriteLine(line);
            }
        }
    }
}
#endif
```

- [ ] **Step 2: 編譯驗證** — Debug build 成功。
- [ ] **Step 3: Commit** — `git commit -m "perf(profiling): add PerfProfilingMiddleware (Debug-only)"`

---

## Task 7: `IToolUtilityProvider` 計時裝飾器（涵蓋靜態 CRM 路徑）

**Files:**
- 先定位介面：Run `grep -rn "interface IToolUtilityProvider" ChurchReport` 取得介面定義（確認 `ToolUtilityClass GetToolUtility()` 簽章與命名空間）。
- Create `Diagnostics/Profiling/TimedToolUtilityProvider.cs`

> `ToolUtility.m_Crm2011OrganizationService` 是 `ToolUtilityClass` 的 **public 可設定欄位**（`ToolUtility/ToolUtilityPartials/ToolUtilityClass.Core.cs:24`）。此裝飾器在每次取得 ToolUtility 時，冪等地把該欄位換成計時版（已是計時版則不重複包），對 singleton 或 per-request 生命週期皆適用。

- [ ] **Step 1: 建立檔案（完整內容；命名空間依 Step 0 確認的介面所在調整）**

```csharp
#if DEBUG
using Microsoft.AspNetCore.Http;
using Microsoft.Xrm.Sdk;
// using <IToolUtilityProvider 與 ToolUtilityClass 的命名空間>;

namespace ChurchReport.Diagnostics.Profiling
{
    /// <summary>
    /// 裝飾 IToolUtilityProvider：確保取回的 ToolUtilityClass 之 m_Crm2011OrganizationService
    /// 為計時版（冪等，不重複包）。涵蓋所有經 ToolUtility 的 CRM 呼叫（僅 Debug）。
    /// </summary>
    public sealed class TimedToolUtilityProvider : IToolUtilityProvider
    {
        private readonly IToolUtilityProvider _inner;
        private readonly IHttpContextAccessor _http;

        public TimedToolUtilityProvider(IToolUtilityProvider inner, IHttpContextAccessor http)
        {
            _inner = inner;
            _http = http;
        }

        public ToolUtilityClass GetToolUtility()
        {
            var tu = _inner.GetToolUtility();
            if (tu != null
                && tu.m_Crm2011OrganizationService != null
                && tu.m_Crm2011OrganizationService is not TimedOrganizationService)
            {
                tu.m_Crm2011OrganizationService =
                    new TimedOrganizationService(tu.m_Crm2011OrganizationService, _http);
            }
            return tu;
        }

        // 若 IToolUtilityProvider 還有其他成員，於此原樣轉呼叫 _inner（Step 0 檢視介面後補齊）。
    }
}
#endif
```

- [ ] **Step 2: 補齊介面其他成員（若有）** — 依 Step 0 的介面定義，把其餘方法/屬性原樣委派給 `_inner`。若介面僅 `GetToolUtility()` 則略過。
- [ ] **Step 3: 編譯驗證** — Debug build 成功。
- [ ] **Step 4: Commit** — `git commit -m "perf(profiling): add TimedToolUtilityProvider to cover static CRM path (Debug-only)"`

---

## Task 8: 連線池路徑包裝 — `BaseChurchController`

**Files:** Modify `Controllers/BaseChurchController.cs`（`GetConnection` 約 987 行、`ReleaseConnection` 約 1033 行）

> 需要 `IHttpContextAccessor`。`BaseChurchController` 可由建構式注入或經 `HttpContext.RequestServices` 取得。以下用 `HttpContext.RequestServices` 取得，避免改動建構式。

- [ ] **Step 1: `GetConnection` 回傳前包裝（`#if DEBUG`）**

在 `var connection = _connectionPool.AcquireConnection();` 之後、`return connection;` 之前插入：

```csharp
#if DEBUG
                var httpAccessor = HttpContext?.RequestServices?
                    .GetService(typeof(Microsoft.AspNetCore.Http.IHttpContextAccessor))
                    as Microsoft.AspNetCore.Http.IHttpContextAccessor;
                if (httpAccessor != null && connection != null
                    && connection is not ChurchReport.Diagnostics.Profiling.TimedOrganizationService)
                {
                    connection = new ChurchReport.Diagnostics.Profiling.TimedOrganizationService(connection, httpAccessor);
                }
#endif
```

- [ ] **Step 2: `ReleaseConnection` 解包（`#if DEBUG`）—— 關鍵：池追蹤的是真實連線**

在 `ReleaseConnection(IOrganizationService connection)` 內，於歸還池之前插入解包：

```csharp
#if DEBUG
            if (connection is ChurchReport.Diagnostics.Profiling.TimedOrganizationService timed)
            {
                connection = timed.Inner;
            }
#endif
```

（確認後續 `_connectionPool.ReleaseConnection(connection);` 收到的是解包後的真實連線。）

- [ ] **Step 3: 編譯驗證** — Debug build 成功。
- [ ] **Step 4: Commit** — `git commit -m "perf(profiling): wrap pooled CRM connection with timing in GetConnection/ReleaseConnection (Debug-only)"`

---

## Task 9: 在 `Startup.cs` 接線（Filter / 中間件 / Provider 裝飾 / 啟動階段）

**Files:** Modify `Startup.cs`（`ConfigureServices` 約 122、`AddMvc` 約 351、`IToolUtilityProvider` 註冊處、`Configure` 約 579）

- [ ] **Step 1: 註冊 Action Filter（`#if DEBUG`）** — 於 `AddMvc(options => { ... })` 內，既有 `options.Filters.Add<ThemeViewDataFilter>();` 附近加入：

```csharp
#if DEBUG
                    options.Filters.Add<ChurchReport.Filters.PerfTimingActionFilter>();
#endif
```

- [ ] **Step 2: 確保 `IHttpContextAccessor` 已註冊** — 若 `ConfigureServices` 尚無 `services.AddHttpContextAccessor();` 則加入（Session_Bleeding 稽核已列其為安全）。Run `grep -n "AddHttpContextAccessor" ChurchReport\Startup.cs` 確認；無則加。

- [ ] **Step 3: 裝飾 `IToolUtilityProvider`（`#if DEBUG`）** — 先 `grep -n "IToolUtilityProvider" ChurchReport\Startup.cs` 找到既有註冊（如 `services.AddScoped<IToolUtilityProvider, XxxProvider>();`）。在其**之後**加入手動裝飾：

```csharp
#if DEBUG
            // 以計時裝飾器包住既有 IToolUtilityProvider（涵蓋 ToolUtility 靜態 CRM 路徑）
            {
                var providerDescriptor = services.LastOrDefault(d => d.ServiceType == typeof(IToolUtilityProvider));
                if (providerDescriptor != null)
                {
                    services.Remove(providerDescriptor);
                    services.Add(new Microsoft.Extensions.DependencyInjection.ServiceDescriptor(
                        typeof(IToolUtilityProvider),
                        sp =>
                        {
                            var inner = (IToolUtilityProvider)ActivatorUtilities.CreateInstance(
                                sp, providerDescriptor.ImplementationType);
                            var http = sp.GetRequiredService<Microsoft.AspNetCore.Http.IHttpContextAccessor>();
                            return new ChurchReport.Diagnostics.Profiling.TimedToolUtilityProvider(inner, http);
                        },
                        providerDescriptor.Lifetime));
                }
            }
#endif
```

> 註：若既有註冊是工廠式（`ImplementationFactory != null`）而非 `ImplementationType`，改用 `providerDescriptor.ImplementationFactory(sp)` 取得 inner。Step 中 grep 結果決定走哪一支；兩種寫法都在此提供。

- [ ] **Step 4: 註冊中間件（`#if DEBUG`，盡量靠前）** — 於 `Configure(...)` 內、既有第一個 `app.UseMiddleware<...>()` 之前（但在 `UseForwardedHeaders`/例外處理之後）加入：

```csharp
#if DEBUG
            app.UseMiddleware<ChurchReport.Middleware.PerfProfilingMiddleware>();
#endif
```

- [ ] **Step 5: `StartupProfiler` 包覆啟動階段（`#if DEBUG`）**
  - `ConfigureServices` 開頭與結尾，用 `using` 包住主體：
    ```csharp
#if DEBUG
            using (ChurchReport.Diagnostics.Profiling.StartupProfiler.Phase("ConfigureServices"))
#endif
            {
                // ... 既有 ConfigureServices 主體 ...
            }
    ```
    （注意：用 `using` 包整個方法主體會新增一層大括號區塊；確認區域變數作用域不受影響後再套。若改動範圍過大，改為在方法開頭 `var __sw = Stopwatch.StartNew();`、結尾 `Debug.WriteLine(...)` 的等價寫法亦可。）
  - `Configure` 同理包一層 `Phase("Configure")`。
  - **連線池預熱**：`grep -n "GetRequiredService<ICrmConnectionPool>\|GetService(typeof(ICrmConnectionPool))" ChurchReport\Startup.cs`。若啟動時有強制解析（預熱）連線池之處，用 `Phase("ConnectionPoolWarmup")` 包住；若無（池為延遲建立），則略過此階段（其成本會顯示在第一個請求的 `[Perf]` conn 時間，不需在此強制預熱——避免改變既有行為）。

- [ ] **Step 6: 編譯驗證** — Debug build 成功，0 錯誤。
- [ ] **Step 7: Commit** — `git commit -m "perf(profiling): wire filter/middleware/provider-decorator/startup phases in Startup (Debug-only)"`

---

## Task 10: 端到端驗證（Debug 行為 + Release 無痕）

**Files:** 無（驗證任務）

- [ ] **Step 1: Debug 建置** — `dotnet build "ChurchReport\ChurchReport.csproj" -c Debug --nologo -v minimal` → 0 警告 0 錯誤。
- [ ] **Step 2: Debug 實跑 + 操作** — 以 Debug 啟動，操作數個功能（至少含「會友資訊」清單）。
- [ ] **Step 3: 驗證輸出** — 於 `Logs\Trace.log`：
  - Run `grep -c "\[Perf\]" ChurchReport\Logs\Trace.log` → > 0（每請求一行）。
  - Run `grep "\[Perf-Startup\]" ChurchReport\Logs\Trace.log` → 有啟動階段行。
  - 交叉驗證：「會友資訊 GetContactImagesBatch」的 `[Perf]` 之 `crm{n,ms}` 與既有 `[BatchImg-Timing]` 同數量級（證明 CRM 計時涵蓋正確，含 ToolUtility 路徑）。
- [ ] **Step 4: Release 建置** — `dotnet build "ChurchReport\ChurchReport.csproj" -c Release --nologo -v minimal` → 0 警告 0 錯誤。
- [ ] **Step 5: Release 無痕驗證** — PowerShell：
  ```powershell
  $dll="ChurchReport\bin\Release\net10.0\ChurchReport.dll"
  $u=[Text.Encoding]::Unicode.GetString([IO.File]::ReadAllBytes($dll))
  "Release 含 [Perf : " + $u.Contains("[Perf")
  ```
  Expected: `False`（Release 組件無任何 `[Perf` 字串）。
- [ ] **Step 6: 連線池健康** — 操作多次後確認無連線錯誤/洩漏（`ReleaseConnection` 解包正確；Trace.log 無連線相關例外）。
- [ ] **Step 7: Commit（若有微調）** — `git commit -m "perf(profiling): verify Debug output and Release no-op"`

---

## Self-Review（撰寫後檢查）

- **Spec 覆蓋**：①每請求摘要→Task 2/6 ②action 時間→Task 5 ③CRM 次數/耗時→Task 4 ④靜態 CRM 路徑→Task 7 ⑤連線池路徑→Task 8 ⑥N+1/慢呼叫明細→Task 2(BuildEscalationLines) ⑦啟動計時→Task 3/9-Step5 ⑧Debug-only/Release 無痕→全程 `#if DEBUG` + Task 10 ⑨Session 安全→只用 `HttpContext.Items`、只記 entity/op/ms。✅ 全覆蓋。
- **Placeholder 掃描**：Task 7/9 含「先 grep 介面/註冊」的引導步驟，但**對應的完整程式碼兩種情況都已給出**（ImplementationType / ImplementationFactory；介面僅 GetToolUtility 或有其他成員），非留白。
- **型別一致性**：`RequestProfiler.ItemsKey`、`RecordCrmCall(entity,op,ms)`、`SetActionElapsed`、`TimedOrganizationService.Inner`、`PerfThresholds.*` 於各任務引用一致。
- **已知引導點**（非 placeholder，但實作時需 1 行 grep 決定分支）：`IToolUtilityProvider` 的命名空間與註冊型態、`AddHttpContextAccessor` 是否已存在、連線池是否啟動預熱。三者皆已提供對應處理。

---

## 完成後：找瓶頸 → 修（迭代，另行進行）

子系統上線後：使用者於 Debug 操作所有功能 → 我讀 `Trace.log` 的 `[Perf]/[Perf-N+1]/[Perf-Gap]/[Perf-Startup]` 依 total/CRM/gap/啟動排序 → 逐一瓶頸：系統化除錯找根因 → 提「session 安全」修法 → 你確認 → 實作 → 驗證計時下降。**每個修正各自獨立評估 Session-Leakage 後再動手。**

---

# ⚠️ v2 修訂（2026-06-17 對抗式檢視後）— 以下覆寫前述任務

> 對抗式檢視找到 8 項改進（詳見 spec v2 修訂重點）。**以本節為準**。覆寫 Task 1/2/4/6/7/8/9，新增 Task 0 與 Task 11。

## Task 0（新增，最先做）：`ProfilingSwitch` runtime 開關

**Files:** Create `Diagnostics/Profiling/ProfilingSwitch.cs`

```csharp
#if DEBUG
namespace ChurchReport.Diagnostics.Profiling
{
    /// <summary>剖析總開關。僅 Debug 編譯存在；再加 runtime 旗標（預設關）雙重保險，
    /// 即使正式機誤以 Debug 部署，未開設定就不剖析。由 Startup 從 Profiling:Enabled 設定。</summary>
    public static class ProfilingSwitch
    {
        public static volatile bool Enabled = false;
    }
}
#endif
```
編譯驗證 → commit `perf(profiling): add ProfilingSwitch runtime gate (Debug-only, default off)`。

## Task 1 覆寫：`PerfThresholds` 增加 `GapMs`

於 `PerfThresholds` 類別內新增：

```csharp
        /// <summary>action − 已計時 CRM 超過此毫秒 → 視為「未歸因時間」過大（盲區/CPU），加印 [Perf-Gap]。</summary>
        public const long GapMs = 150;
```

## Task 2 覆寫：`RequestProfiler`（ticks 精度 + gap）

以下**完整內容**取代 `Diagnostics/Profiling/RequestProfiler.cs`：

```csharp
#if DEBUG
using System.Collections.Generic;
using System.Diagnostics;
using System.Text;

namespace ChurchReport.Diagnostics.Profiling
{
    /// <summary>單一請求計時容器。只放 HttpContext.Items，請求結束即回收。
    /// 只記數字 + entity 邏輯名 + 操作名。CRM 時間以 ticks 累計避免 &lt;1ms 捨入低報。</summary>
    public sealed class RequestProfiler
    {
        public const string ItemsKey = "__PerfProfiler";
        private static readonly double TicksToMs = 1000.0 / Stopwatch.Frequency;

        private readonly Stopwatch _total = Stopwatch.StartNew();
        private readonly object _lock = new object();
        private long _actionMs;
        private int _crmCount;
        private long _crmTicks;                       // 累計 ticks（高精度）
        private string _slowestEntity;
        private string _slowestOp;
        private long _slowestMs;
        private readonly Dictionary<string, (int count, long ms)> _crmByOp =
            new Dictionary<string, (int, long)>();
        private readonly List<(string entity, string op, long ms)> _slowCalls =
            new List<(string, string, long)>();

        public void SetActionElapsed(long ms) => _actionMs = ms;

        /// <param name="elapsedTicks">由裝飾器以 Stopwatch.GetTimestamp() 差值傳入。</param>
        public void RecordCrmCall(string entity, string op, long elapsedTicks)
        {
            entity ??= "?";
            op ??= "?";
            long ms = (long)(elapsedTicks * TicksToMs);
            lock (_lock)
            {
                _crmCount++;
                _crmTicks += elapsedTicks;
                if (ms > _slowestMs) { _slowestMs = ms; _slowestEntity = entity; _slowestOp = op; }
                var key = entity + "." + op;
                _crmByOp[key] = _crmByOp.TryGetValue(key, out var cur)
                    ? (cur.count + 1, cur.ms + ms) : (1, ms);
                if (ms > PerfThresholds.SlowSingleCallMs) _slowCalls.Add((entity, op, ms));
            }
        }

        public long StopAndGetTotalMs() { _total.Stop(); return _total.ElapsedMilliseconds; }

        private long CrmMs => (long)(_crmTicks * TicksToMs);
        private long Gap => (_actionMs - CrmMs) > 0 ? (_actionMs - CrmMs) : 0;

        public string BuildSummaryLine(string path, long totalMs)
        {
            var slowest = _slowestMs > 0 ? $" slowest={_slowestEntity}.{_slowestOp}:{_slowestMs}ms" : "";
            return $"[Perf] path={path} total={totalMs}ms action={_actionMs}ms "
                 + $"crm{{n={_crmCount},ms={CrmMs}}} gap={Gap}ms{slowest}";
        }

        public IEnumerable<string> BuildEscalationLines(string path)
        {
            var lines = new List<string>();
            if (_crmCount > PerfThresholds.NPlusOneCrmCount)
            {
                var sb = new StringBuilder();
                lock (_lock)
                    foreach (var kv in _crmByOp)
                        sb.Append($"{kv.Key} ×{kv.Value.count} (Σ{kv.Value.ms}ms), ");
                lines.Add($"[Perf-N+1] path={path} crm.n={_crmCount} 詳列: "
                        + sb.ToString().TrimEnd(',', ' '));
            }
            lock (_lock)
                foreach (var c in _slowCalls)
                    lines.Add($"[Perf-Slow] path={path} {c.entity}.{c.op} {c.ms}ms");
            if (Gap > PerfThresholds.GapMs)
                lines.Add($"[Perf-Gap] path={path} action={_actionMs}ms crm.ms={CrmMs} gap={Gap}ms "
                        + "(未歸因:可能 m_OrganizationService proxy 路徑或非 CRM 運算)");
            return lines;
        }
    }
}
#endif
```

## Task 4 覆寫：`TimedOrganizationService` 改傳 ticks

把 Task 4 的兩個 `Time` 私有方法替換為（其餘不變）：

```csharp
        private T Time<T>(string entity, string op, System.Func<T> call)
        {
            var start = Stopwatch.GetTimestamp();
            try { return call(); }
            finally { Profiler?.RecordCrmCall(entity, op, Stopwatch.GetTimestamp() - start); }
        }

        private void Time(string entity, string op, System.Action call)
        {
            var start = Stopwatch.GetTimestamp();
            try { call(); }
            finally { Profiler?.RecordCrmCall(entity, op, Stopwatch.GetTimestamp() - start); }
        }
```

## Task 6 覆寫：`PerfProfilingMiddleware`（開關 + 路由樣板 + gap）

以下**完整內容**取代 `Middleware/PerfProfilingMiddleware.cs`：

```csharp
#if DEBUG
using System.Diagnostics;
using System.Threading.Tasks;
using ChurchReport.Diagnostics.Profiling;
using Microsoft.AspNetCore.Http;
using Microsoft.AspNetCore.Routing;

namespace ChurchReport.Middleware
{
    public sealed class PerfProfilingMiddleware
    {
        private readonly RequestDelegate _next;
        public PerfProfilingMiddleware(RequestDelegate next) => _next = next;

        public async Task Invoke(HttpContext context)
        {
            if (!ProfilingSwitch.Enabled) { await _next(context); return; } // runtime 開關

            var profiler = new RequestProfiler();
            context.Items[RequestProfiler.ItemsKey] = profiler;
            try
            {
                await _next(context);
            }
            finally
            {
                var totalMs = profiler.StopAndGetTotalMs();
                var path = GetRouteTemplate(context); // 樣板，不含實際 ID
                Debug.WriteLine(profiler.BuildSummaryLine(path, totalMs));
                foreach (var line in profiler.BuildEscalationLines(path))
                    Debug.WriteLine(line);
            }
        }

        // finally 在 _next 之後執行，此時路由已跑完，GetEndpoint() 可取得對應端點的路由樣板。
        private static string GetRouteTemplate(HttpContext ctx)
        {
            if (ctx.GetEndpoint() is RouteEndpoint re && !string.IsNullOrEmpty(re.RoutePattern?.RawText))
                return "/" + re.RoutePattern.RawText.TrimStart('/');
            return ctx.Request?.Path.Value ?? "?";
        }
    }
}
#endif
```

## Task 7 覆寫：`TimedToolUtilityProvider.GetToolUtility()`（開關 + 冪等鎖防競態）

把 `GetToolUtility()` 改為（類別內另加 `private static readonly object _wrapLock = new object();`）：

```csharp
        private static readonly object _wrapLock = new object();

        public ToolUtilityClass GetToolUtility()
        {
            var tu = _inner.GetToolUtility();
            if (ProfilingSwitch.Enabled && tu != null
                && tu.m_Crm2011OrganizationService != null
                && tu.m_Crm2011OrganizationService is not TimedOrganizationService)
            {
                lock (_wrapLock) // 防 singleton 共用欄位並發首呼造成雙重包裝
                {
                    var svc = tu.m_Crm2011OrganizationService;
                    if (svc != null && svc is not TimedOrganizationService)
                        tu.m_Crm2011OrganizationService = new TimedOrganizationService(svc, _http);
                }
            }
            return tu;
        }
```

## Task 8 覆寫：`GetConnection` 包裝加開關

把 Task 8 Step 1 的包裝條件改為（加 `ProfilingSwitch.Enabled &&`）：

```csharp
#if DEBUG
                if (ChurchReport.Diagnostics.Profiling.ProfilingSwitch.Enabled)
                {
                    var httpAccessor = HttpContext?.RequestServices?
                        .GetService(typeof(Microsoft.AspNetCore.Http.IHttpContextAccessor))
                        as Microsoft.AspNetCore.Http.IHttpContextAccessor;
                    if (httpAccessor != null && connection != null
                        && connection is not ChurchReport.Diagnostics.Profiling.TimedOrganizationService)
                    {
                        connection = new ChurchReport.Diagnostics.Profiling.TimedOrganizationService(connection, httpAccessor);
                    }
                }
#endif
```
（`ReleaseConnection` 的解包不變——即使開關關閉，連線本就不是裝飾器，解包判斷自然略過。）

## Task 9 覆寫：Startup 接線

- **Step A（新增）**：`ConfigureServices` 開頭設定開關：
  ```csharp
#if DEBUG
            ChurchReport.Diagnostics.Profiling.ProfilingSwitch.Enabled =
                Configuration.GetValue<bool>("Profiling:Enabled", false);
#endif
  ```
- **Step B（覆寫原 Step 4）**：中間件**改註冊在 `app.UseStaticFiles(...)`（約 716 行）之後**（避免靜態檔灌爆 log），其餘 `#if DEBUG` 不變：
  ```csharp
#if DEBUG
            app.UseMiddleware<ChurchReport.Middleware.PerfProfilingMiddleware>();
#endif
  ```
- **Step C（新增）**：於 `appsettings.Development.json` 加入（預設關；要剖析時改 `true`）：
  ```json
  "Profiling": { "Enabled": false }
  ```
  （不要加進 `appsettings.Production.json`／`appsettings.json`，正式設定維持無此鍵＝關。）

## Task 11（新增）：分析腳本 `Tools\parse-perf-log.ps1`

**Files:** Create `Tools\parse-perf-log.ps1`

```powershell
# 彙總 Trace.log 的 [Perf] 行：找最慢端點 / 疑似 N+1 / 盲區(gap)。
# 用法：powershell -File Tools\parse-perf-log.ps1 -Log Logs\Trace.log -Top 20
param([string]$Log = "Logs\Trace.log", [int]$Top = 20)

$rx = [regex]'\[Perf\] path=(?<path>\S+) total=(?<total>\d+)ms action=(?<action>\d+)ms crm\{n=(?<n>\d+),ms=(?<crm>\d+)\} gap=(?<gap>\d+)ms'
$rows = Get-Content -LiteralPath $Log | ForEach-Object {
    $m = $rx.Match($_)
    if ($m.Success) {
        [pscustomobject]@{
            Path = $m.Groups['path'].Value
            Total = [int]$m.Groups['total'].Value
            Action = [int]$m.Groups['action'].Value
            CrmN = [int]$m.Groups['n'].Value
            CrmMs = [int]$m.Groups['crm'].Value
            Gap = [int]$m.Groups['gap'].Value
        }
    }
}

if (-not $rows) { Write-Host "找不到 [Perf] 行（確認已開 Profiling:Enabled 並以 Debug 跑過）"; return }

Write-Host "=== 依端點彙總（最慢在上）===" -ForegroundColor Cyan
$rows | Group-Object Path | ForEach-Object {
    [pscustomobject]@{
        Path = $_.Name; Hits = $_.Count
        AvgTotal = [int]($_.Group | Measure-Object Total -Average).Average
        MaxTotal = ($_.Group | Measure-Object Total -Maximum).Maximum
        MaxCrmN = ($_.Group | Measure-Object CrmN -Maximum).Maximum
        MaxGap = ($_.Group | Measure-Object Gap -Maximum).Maximum
    }
} | Sort-Object MaxTotal -Descending | Select-Object -First $Top | Format-Table -AutoSize

Write-Host "=== 疑似 N+1（單次 crm.n 最高）===" -ForegroundColor Yellow
$rows | Sort-Object CrmN -Descending | Select-Object -First $Top Path, Total, CrmN, CrmMs, Gap | Format-Table -AutoSize

Write-Host "=== 盲區（gap 最大：proxy 路徑或 CPU）===" -ForegroundColor Magenta
$rows | Sort-Object Gap -Descending | Select-Object -First $Top Path, Total, Action, CrmMs, Gap | Format-Table -AutoSize
```
編譯不適用；commit `perf(profiling): add parse-perf-log.ps1 analysis script`。

## v2 驗收補充（併入 Task 10）

- [ ] **開關驗證**：`Profiling:Enabled=false` 時，Debug 跑 → `Trace.log` **無** `[Perf]` 行；改 `true` 後才有。
- [ ] **路由樣板**：含 ID 的端點（如會員明細）其 `[Perf] path=` 顯示 `{id}` 樣板，**不含實際 GUID**。
- [ ] **gap 偵測**：對已知走 proxy 路徑或重 CPU 的端點，確認出現 `[Perf-Gap]`。
- [ ] **靜態檔**：載入頁面後 `Trace.log` **無** css/js/圖的 `[Perf]` 行。
- [ ] **分析腳本**：`Tools\parse-perf-log.ps1` 能正確列出三張排序表。
