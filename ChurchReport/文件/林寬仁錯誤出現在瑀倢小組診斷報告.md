# 林寬仁錯誤出現在瑀倢小組問題診斷報告

## ?? 問題描述

**症狀**: 林寬仁出現在瑀倢小組的成員列表中，但他不應該屬於這個小組

**影響範圍**: 
- 裝備狀態頁面 (EquipmentView)
- 可能影響其他小組報表

---

## ?? 可能原因分析

### 原因 1: 週報資料問題（最可能）

**位置**: `GetAllMemberDataFromPresentRecord` 方法

**問題**: 如果瑀倢小組的週報（`new_group_present_weekly_report`）中包含了林寬仁的出席記錄（`new_present_record`），他就會被載入到瑀倢小組

```csharp
private void GetAllMemberDataFromPresentRecord(String GroupName, Guid WeeklyReportId, ref ListSmallGroupWeeklyReport aListSmallGroupWeeklyReport)
{
    // 搜尋這個週報裡的所有個人聚會與靈修記錄集合
    EntityCollection PresentRecordCollection = GetPresentRecordByLoginType(GroupName, WeeklyReportId, ref aListSmallGroupWeeklyReport);
    
    // ? 問題: 這裡會載入週報中的所有出席記錄
    // 如果林寬仁的出席記錄被錯誤地關聯到瑀倢小組的週報，他就會出現
    foreach (Entity PresentRecordEntity in PresentRecordCollection.Entities)
    {
        // ... 將成員添加到 aListSmallGroupWeeklyReport.m_SmallGroupDataList.m_AllMemeberData.Members
    }
}
```

**診斷步驟**:
1. 檢查瑀倢小組的週報 ID
2. 查詢該週報關聯的所有出席記錄
3. 確認林寬仁的出席記錄是否錯誤地關聯到這個週報

### 原因 2: 名單資料問題

**位置**: `GetAllMemberDataFromList` 方法

**問題**: 如果林寬仁被錯誤地加入到瑀倢小組的名單（`list`）中

```csharp
private void GetAllMemberDataFromList(String GroupName, Guid ListEntityId, ref ListSmallGroupWeeklyReport aListSmallGroupWeeklyReport)
{
    Entity ListEntity = this.m_ToolUtilityClass.RetrieveEntity("list", ListEntityId);
    
    bool ListType = this.m_ToolUtilityClass.GetEntityBoolAttribute(ListEntity, "type");
    EntityCollection MemberCollection;
    
    if (ListType == false)
    {
        // 靜態名單
        MemberCollection = this.m_ToolUtilityClass.RetrieveMemberListCollectionByListIdDynamics365(ref this.m_ToolUtilityClass.m_OrganizationService, ListEntityId);
    }
    else
    {
        // 動態名單
        MemberCollection = this.m_ToolUtilityClass.RetrieveDynamicMemberListDynamics365(ref this.m_ToolUtilityClass.m_OrganizationService, ListEntityId);
    }
    
    // ? 問題: 如果林寬仁在瑀倢小組的名單中，他就會被載入
    foreach (Entity MemberEntity in MemberCollection.Entities)
    {
        // ... 將成員添加到列表
    }
}
```

### 原因 3: 小組 ID 混淆

**問題**: 多小組切換時，`ListEntityId` 可能沒有正確更新

**相關修復**: 已在 `ListManager.SetupIntegrateData` 中修復 `ActiveListId` 更新問題

---

## ?? 診斷步驟

### 步驟 1: 確認林寬仁的基本資料

在 CRM 中檢查：

1. **聯絡人 (Contact)**
   - 姓名: 林寬仁
   - ContactId: `{查詢}`
   - 委身類型: `{查詢}`

2. **所屬小組名單 (List)**
   - 林寬仁應該屬於哪個小組？
   - 查詢他在哪些名單中

```sql
-- FetchXML 查詢林寬仁所屬的所有名單
<fetch version='1.0'>
  <entity name='list'>
    <attribute name='listname' />
    <attribute name='listid' />
    <link-entity name='listmember' from='listid' to='listid'>
      <link-entity name='contact' from='contactid' to='entityid'>
        <filter>
          <condition attribute='fullname' operator='eq' value='林寬仁' />
        </filter>
      </link-entity>
    </link-entity>
  </entity>
</fetch>
```

### 步驟 2: 檢查瑀倢小組的名單

在 CRM 中檢查瑀倢小組：

1. **小組名單 (List)**
   - 名單名稱: 瑀倢小組（或類似名稱）
   - ListId: `{查詢}`

2. **名單成員**
   - 查詢該名單的所有成員
   - 確認林寬仁是否在列表中

```sql
-- FetchXML 查詢瑀倢小組的所有成員
<fetch version='1.0'>
  <entity name='contact'>
    <attribute name='fullname' />
    <attribute name='contactid' />
    <link-entity name='listmember' from='entityid' to='contactid'>
      <link-entity name='list' from='listid' to='listid'>
        <filter>
          <condition attribute='listname' operator='like' value='%瑀倢%' />
        </filter>
      </link-entity>
    </link-entity>
  </entity>
</fetch>
```

### 步驟 3: 檢查週報關聯

在 CRM 中檢查瑀倢小組的週報：

1. **週報 (new_group_present_weekly_report)**
   - 小組: 瑀倢小組
   - 週報 ID: `{查詢}`
   - 主日日期: `{當前查詢的日期}`

2. **出席記錄 (new_present_record)**
   - 查詢該週報關聯的所有出席記錄
   - 確認林寬仁的出席記錄是否存在

```sql
-- FetchXML 查詢瑀倢小組週報中的所有出席記錄
<fetch version='1.0'>
  <entity name='new_present_record'>
    <attribute name='new_name' />
    <attribute name='new_contact_new_present_record' />
    <link-entity name='new_group_present_weekly_report' from='new_group_present_weekly_reportid' to='new_group_present_weekly_report_prese'>
      <link-entity name='list' from='listid' to='new_list_weekly_report'>
        <filter>
          <condition attribute='listname' operator='like' value='%瑀倢%' />
        </filter>
      </link-entity>
    </link-entity>
  </entity>
</fetch>
```

### 步驟 4: 檢查出席記錄的小組關聯

檢查林寬仁的出席記錄：

```sql
-- FetchXML 查詢林寬仁的所有出席記錄及其關聯的小組
<fetch version='1.0'>
  <entity name='new_present_record'>
    <attribute name='new_name' />
    <attribute name='new_sunday_date' />
    <attribute name='new_list_new_present_record' />
    <attribute name='new_group_present_weekly_report_prese' />
    <link-entity name='contact' from='contactid' to='new_contact_new_present_record'>
      <filter>
        <condition attribute='fullname' operator='eq' value='林寬仁' />
      </filter>
    </link-entity>
    <link-entity name='list' from='listid' to='new_list_new_present_record' alias='list'>
      <attribute name='listname' />
    </link-entity>
  </entity>
</fetch>
```

---

## ?? 可能的修復方案

### 方案 1: 修正 CRM 資料（最常見）

**如果發現問題在 CRM**:

1. **從瑀倢小組名單中移除林寬仁**
   - 導航到: CRM → 行銷 → 名單 → 瑀倢小組
   - 從成員中移除林寬仁

2. **修正出席記錄的小組關聯**
   - 找到林寬仁錯誤關聯到瑀倢小組的出席記錄
   - 修改 `new_list_new_present_record` 欄位，指向正確的小組
   - 修改 `new_group_present_weekly_report_prese` 欄位，指向正確的週報

3. **檢查週報關聯**
   - 如果瑀倢小組的週報錯誤地包含林寬仁的出席記錄
   - 將該出席記錄移除或重新關聯到正確的週報

### 方案 2: 添加程式碼驗證（防禦性）

在 `GetAllMemberDataFromPresentRecord` 方法中添加驗證：

```csharp
private void GetAllMemberDataFromPresentRecord(String GroupName, Guid WeeklyReportId, ref ListSmallGroupWeeklyReport aListSmallGroupWeeklyReport)
{
    EntityCollection PresentRecordCollection = GetPresentRecordByLoginType(GroupName, WeeklyReportId, ref aListSmallGroupWeeklyReport);

    foreach (Entity PresentRecordEntity in PresentRecordCollection.Entities)
    {
        // ... 現有代碼 ...
        
        // ? 添加驗證: 確認出席記錄的小組名單與當前小組一致
        Guid presentRecordListId = this.m_ToolUtilityClass.GetEntityLookupAttribute(PresentRecordEntity, "new_list_new_present_record");
        
        if (presentRecordListId != this.m_ListEntity.Id)
        {
            System.Diagnostics.Debug.WriteLine($"[警告] 出席記錄 {PresentRecordEntity.Id} 的小組 ({presentRecordListId}) 與當前小組 ({this.m_ListEntity.Id}) 不一致，跳過此記錄");
            continue; // 跳過不屬於當前小組的出席記錄
        }
        
        // ... 繼續處理 ...
    }
}
```

### 方案 3: 添加日誌記錄

在載入成員時添加詳細日誌：

```csharp
System.Diagnostics.Debug.WriteLine($"[GetAllMemberDataFromPresentRecord] 小組: {GroupName}, 週報ID: {WeeklyReportId}");
System.Diagnostics.Debug.WriteLine($"[GetAllMemberDataFromPresentRecord] 出席記錄數量: {PresentRecordCollection.Entities.Count}");

foreach (Entity PresentRecordEntity in PresentRecordCollection.Entities)
{
    String FullName = "";
    if (PresentRecordEntity.Attributes.Contains("new_contact_new_present_record"))
    {
        EntityReference contactRef = (EntityReference)PresentRecordEntity.Attributes["new_contact_new_present_record"];
        FullName = contactRef.Name;
    }
    
    Guid presentRecordListId = this.m_ToolUtilityClass.GetEntityLookupAttribute(PresentRecordEntity, "new_list_new_present_record");
    Guid currentListId = this.m_ListEntity.Id;
    
    System.Diagnostics.Debug.WriteLine($"[成員] {FullName}");
    System.Diagnostics.Debug.WriteLine($"  - 出席記錄ID: {PresentRecordEntity.Id}");
    System.Diagnostics.Debug.WriteLine($"  - 出席記錄關聯小組ID: {presentRecordListId}");
    System.Diagnostics.Debug.WriteLine($"  - 當前小組ID: {currentListId}");
    System.Diagnostics.Debug.WriteLine($"  - 是否匹配: {presentRecordListId == currentListId}");
    
    if (FullName == "林寬仁")
    {
        System.Diagnostics.Debug.WriteLine($"[特別注意] 發現林寬仁！");
        System.Diagnostics.Debug.WriteLine($"  - 當前小組: {GroupName}");
        System.Diagnostics.Debug.WriteLine($"  - 出席記錄關聯小組: {presentRecordListId}");
    }
}
```

---

## ?? 測試驗證

### 測試場景 1: 確認問題

**步驟**:
1. 訪問 `/Equipment/EquipmentView`
2. 展開瑀倢小組
3. 查看成員列表

**預期結果**:
- ? 當前: 林寬仁出現在列表中
- ? 修正後: 林寬仁不應該出現

### 測試場景 2: 檢查林寬仁的正確小組

**步驟**:
1. 確認林寬仁應該屬於哪個小組
2. 展開該小組
3. 確認林寬仁出現在正確的小組中

**預期結果**:
- ? 林寬仁出現在他所屬的小組中
- ? 不會重複出現在多個小組

### 測試場景 3: 檢查其他成員

**步驟**:
1. 檢查其他小組
2. 確認沒有成員錯誤地出現在不屬於他們的小組中

**預期結果**:
- ? 所有成員都出現在正確的小組中
- ? 沒有重複或錯誤的成員顯示

---

## ?? 進階診斷工具

### PowerShell 診斷腳本

創建 `診斷林寬仁小組問題.ps1`:

```powershell
# 診斷林寬仁小組問題
Write-Host "=====================================" -ForegroundColor Cyan
Write-Host "林寬仁小組歸屬診斷工具" -ForegroundColor Cyan
Write-Host "=====================================" -ForegroundColor Cyan
Write-Host ""

# 設定 CRM 連線資訊（需要根據實際環境修改）
$crmUrl = "https://your-crm-url.dynamics.com"
$contactName = "林寬仁"
$targetGroupName = "瑀倢"

Write-Host "診斷資訊:" -ForegroundColor Yellow
Write-Host "  聯絡人: $contactName" -ForegroundColor Gray
Write-Host "  檢查小組: $targetGroupName" -ForegroundColor Gray
Write-Host ""

Write-Host "請在 CRM 中執行以下檢查:" -ForegroundColor White
Write-Host ""

Write-Host "1. 檢查聯絡人所屬名單:" -ForegroundColor Green
Write-Host "   進階尋找 → 名單 → 新增相關 → 名單成員 → 新增相關 → 聯絡人" -ForegroundColor Gray
Write-Host "   篩選條件: 全名 = '$contactName'" -ForegroundColor Gray
Write-Host ""

Write-Host "2. 檢查瑀倢小組成員:" -ForegroundColor Green
Write-Host "   進階尋找 → 聯絡人 → 新增相關 → 名單成員 → 新增相關 → 名單" -ForegroundColor Gray
Write-Host "   篩選條件: 名單名稱 包含 '$targetGroupName'" -ForegroundColor Gray
Write-Host ""

Write-Host "3. 檢查出席記錄關聯:" -ForegroundColor Green
Write-Host "   進階尋找 → 個人聚會與靈修記錄 → 新增相關 → 聯絡人" -ForegroundColor Gray
Write-Host "   篩選條件: 全名 = '$contactName'" -ForegroundColor Gray
Write-Host "   顯示欄位: 小組名單、週報" -ForegroundColor Gray
Write-Host ""

Write-Host "4. 檢查是否有重複記錄:" -ForegroundColor Green
Write-Host "   如果 $contactName 出現在多個小組的出席記錄中" -ForegroundColor Gray
Write-Host "   需要修正 '小組名單' 欄位，指向正確的小組" -ForegroundColor Gray
Write-Host ""

Write-Host "=====================================" -ForegroundColor Cyan
Write-Host "修復建議" -ForegroundColor Cyan
Write-Host "=====================================" -ForegroundColor Cyan
Write-Host ""

Write-Host "如果發現問題，請執行以下步驟:" -ForegroundColor White
Write-Host ""
Write-Host "A. 移除錯誤的名單成員:" -ForegroundColor Yellow
Write-Host "   1. 導航到 行銷 → 名單 → {瑀倢小組}" -ForegroundColor Gray
Write-Host "   2. 點擊 '成員' 標籤" -ForegroundColor Gray
Write-Host "   3. 找到 $contactName" -ForegroundColor Gray
Write-Host "   4. 選擇並點擊 '從名單移除'" -ForegroundColor Gray
Write-Host ""

Write-Host "B. 修正出席記錄關聯:" -ForegroundColor Yellow
Write-Host "   1. 找到 $contactName 錯誤關聯到瑀倢小組的出席記錄" -ForegroundColor Gray
Write-Host "   2. 開啟該記錄" -ForegroundColor Gray
Write-Host "   3. 修改 '小組名單' 欄位，選擇正確的小組" -ForegroundColor Gray
Write-Host "   4. 修改 '週報' 欄位，選擇正確的週報" -ForegroundColor Gray
Write-Host "   5. 儲存" -ForegroundColor Gray
Write-Host ""

Write-Host "C. 驗證修復:" -ForegroundColor Yellow
Write-Host "   1. 重新訪問 裝備狀態頁面" -ForegroundColor Gray
Write-Host "   2. 展開瑀倢小組" -ForegroundColor Gray
Write-Host "   3. 確認 $contactName 不再出現" -ForegroundColor Gray
Write-Host "   4. 展開 $contactName 正確的小組" -ForegroundColor Gray
Write-Host "   5. 確認 $contactName 出現在正確的小組中" -ForegroundColor Gray
Write-Host ""

Write-Host "=====================================" -ForegroundColor Cyan
Write-Host "診斷完成" -ForegroundColor Cyan
Write-Host "=====================================" -ForegroundColor Cyan
```

---

## ?? 快速檢查清單

### CRM 資料檢查

- [ ] 確認林寬仁的 ContactId
- [ ] 確認林寬仁所屬的小組名單
- [ ] 檢查瑀倢小組的名單成員
- [ ] 檢查瑀倢小組的週報
- [ ] 檢查林寬仁的出席記錄關聯

### 程式碼檢查

- [ ] 檢查 `GetAllMemberDataFromPresentRecord` 方法
- [ ] 檢查 `GetAllMemberDataFromList` 方法
- [ ] 檢查 `SetupIntegrateData` 方法（ActiveListId 更新）
- [ ] 添加日誌記錄
- [ ] 添加資料驗證

### 測試驗證

- [ ] 確認問題存在
- [ ] 修正 CRM 資料
- [ ] 重新測試瑀倢小組
- [ ] 測試林寬仁的正確小組
- [ ] 測試其他小組

---

## ?? 最可能的原因

根據經驗，**最可能的原因是**:

### 出席記錄的小組關聯錯誤

林寬仁有一筆出席記錄（`new_present_record`）被錯誤地關聯到瑀倢小組的週報。

**為什麼會發生**:
1. 手動建立出席記錄時選錯小組
2. 批次匯入資料時資料錯誤
3. 之前修改過林寬仁的小組，但舊的出席記錄沒有更新

**如何修正**:
1. 在 CRM 中找到該出席記錄
2. 修改 `new_list_new_present_record` 欄位
3. 修改 `new_group_present_weekly_report_prese` 欄位
4. 儲存

**預防措施**:
- 在程式碼中添加驗證，確保出席記錄的小組與當前小組一致
- 添加日誌，記錄所有載入的成員及其來源

---

## ?? 相關文檔

- `多小組由下往上點擊問題最終修復報告.md` - ListManager ActiveListId 修復
- `多小組顯示相同聯絡人問題修復報告.md` - 類似的資料混淆問題
- `EquipmentController重構文檔.md` - 控制器架構

---

**診斷日期**: 2024-11-18  
**問題等級**: 中  
**影響範圍**: 裝備狀態成員顯示  
**最可能原因**: 出席記錄的小組關聯錯誤  
**推薦方案**: 修正 CRM 資料 + 添加程式碼驗證
