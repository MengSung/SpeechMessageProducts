# PR-04 執行狀態報告

## 當前狀態：?? 暫停（檔案過大）

**原因**：`ToolUtilityClass.cs` 檔案超過 10,000 行，編輯器無法一次性處理完整修改。

---

## 已完成的準備工作

### ? PR-03 完成
- [x] 建立 `ICrmClient` 介面
- [x] 建立 `DataverseServiceClientAdapter`（條件編譯）
- [x] 建立 `LegacyOrganizationServiceAdapter`
- [x] 建立 `CrmClientFactory`
- [x] 驗證編譯成功（0 errors）

### ? PR-04 規劃完成
- [x] 建立 `PR-04_資源洩漏修正計畫.md`
- [x] 詳細分析當前問題（FileStream / TraceListener 洩漏）
- [x] 設計改造方案（方案 A: 激進式，完全改用 ILogger）
- [x] 準備驗收標準（CA2000 / 檔案鎖定測試 / Memory Leak 檢測）

---

## PR-04 需要的修改（分批執行）

由於 `ToolUtilityClass.cs` 檔案過大（約 10,000+ 行），建議分批手動修改：

### 批次 1: 加入新的 using 與欄位（行 1-120）

```csharp
// 在 using 區塊加入：
using Microsoft.Extensions.Logging;
using Microsoft.Extensions.Logging.Abstractions;
using ToolUtilityNameSpace.Interfaces;

// 在 class 開頭加入新欄位（約行 40-50）：
private readonly ILogger<ToolUtilityClass> _logger;
private readonly ICrmClient _crmClient;
private readonly IConfiguration _configuration;

// 標註舊欄位為 Obsolete（約行 110-118）：
[Obsolete("請使用 ILogger<ToolUtilityClass>，此欄位將在未來版本移除")]
private String m_TraceLogFile = "";

[Obsolete("請使用 ILogger<ToolUtilityClass>，此欄位將在未來版本移除")]
private BugslayerTextWriterTraceListener m_Listener = new BugslayerTextWriterTraceListener();

[Obsolete("請使用 ILogger<ToolUtilityClass>，此欄位將在未來版本移除")]
private FileStream m_XmlFileStream;

[Obsolete("請使用 ILogger<ToolUtilityClass>，此欄位將在未來版本移除")]
private StreamWriter m_XmlFileStreamWriter;
```

### 批次 2: 新增主要建構式（插入到行 123 之前）

```csharp
/// <summary>
/// 建構式（支援 DI 注入）
/// </summary>
/// <param name="logger">ILogger 實例（推薦使用）</param>
/// <param name="crmClient">ICrmClient 實例（可選，null 時使用舊方式）</param>
/// <param name="configuration">IConfiguration 實例（可選）</param>
public ToolUtilityClass(
    ILogger<ToolUtilityClass> logger = null,
    ICrmClient crmClient = null,
    IConfiguration configuration = null)
{
    // 使用 NullLogger 避免 null reference
    _logger = logger ?? NullLogger<ToolUtilityClass>.Instance;
    _crmClient = crmClient;
    _configuration = configuration;

    _logger.LogInformation("ToolUtilityClass 正在初始化...");

    // 向後相容：若未注入 CrmClient，使用舊方式
    if (_crmClient == null)
    {
        _logger.LogWarning("未注入 ICrmClient，使用舊的 OnPremiseClient 方式（不建議）");
        
        var adUrl = "https://" + ORGANIZATION + ".speechmessage.com.tw/XRMServices/2011/Organization.svc";
        var adUsername = @"SPEECHMESSAGE\Administrator";
        var adPassword = "hu9840";

        m_Crm2011OrganizationService = new OnPremiseClient(adUrl, adUsername, adPassword);
    }
    else
    {
        _logger.LogInformation("使用注入的 ICrmClient（推薦方式）");
    }

    _logger.LogInformation("ToolUtilityClass 初始化完成");
}
```

### 批次 3: 修改現有建構式（行 123-210）

```csharp
/// <summary>
/// 舊的無參數建構式（向後相容，不建議使用）
/// </summary>
[Obsolete("請使用帶有 ILogger 參數的建構式，此建構式將在未來版本移除")]
public ToolUtilityClass() : this(null, null, null)
{
    // 為了向後相容，暫時保留舊的 FileStream trace 初始化
    InitializeLegacyTrace();
}

/// <summary>
/// 初始化舊的 FileStream trace（僅用於向後相容）
/// </summary>
private void InitializeLegacyTrace()
{
    try
    {
        // 只有在沒有 ILogger 的情況下才初始化舊的 trace
        if (_logger is NullLogger<ToolUtilityClass>)
        {
            m_TraceLogFile = TRACE_DIRECTOR;
            m_XmlFileStream = new FileStream(m_TraceLogFile, FileMode.Append, FileAccess.Write, FileShare.ReadWrite);
            m_XmlFileStreamWriter = new StreamWriter(m_XmlFileStream, Encoding.GetEncoding("big5"));
            m_Listener = new BugslayerTextWriterTraceListener(m_XmlFileStreamWriter);

            Debug.AutoFlush = true;
            Debug.Listeners.Add(m_Listener);
        }
    }
    catch (Exception ex)
    {
        Debug.WriteLine($"初始化舊 trace 失敗: {ex.Message}");
    }
}
```

### 批次 4: 修正 Dispose 方法（行 210-223）

**完全替換現有的 Dispose 方法**：

```csharp
protected virtual void Dispose(bool disposing)
{
    if (_disposed) return;

    if (disposing)
    {
        // 1. 釋放 ICrmClient（若有）
        _crmClient?.Dispose();

        // 2. 釋放舊的 OrganizationService（若有）
        if (m_OrganizationService is IDisposable disposableOrgService)
        {
            disposableOrgService.Dispose();
        }

        // 3. 釋放舊的 FileStream / TraceListener（若有）
        if (m_Listener != null)
        {
            Debug.Listeners.Remove(m_Listener);
            m_Listener.Dispose();
            m_Listener = null;
        }

        if (m_XmlFileStreamWriter != null)
        {
            m_XmlFileStreamWriter.Flush();
            m_XmlFileStreamWriter.Dispose();
            m_XmlFileStreamWriter = null;
        }

        if (m_XmlFileStream != null)
        {
            m_XmlFileStream.Dispose();
            m_XmlFileStream = null;
        }

        _logger?.LogInformation("ToolUtilityClass 已釋放所有資源");
    }

    _disposed = true;
}

public void Dispose()
{
    Dispose(true);
    GC.SuppressFinalize(this);
}
```

### 批次 5: 更新 TraceByLevel 方法（最後）

找到 `TraceByLevel` 方法（約在檔案末尾），修改為：

```csharp
public void TraceByLevel(Int32 TotalLevel, Int32 QualifiedLevel, String StringToProcess)
{
    try
    {
        if (TotalLevel >= QualifiedLevel)
        {
            // 優先使用 ILogger
            if (_logger != null && !(_logger is NullLogger<ToolUtilityClass>))
            {
                var stackTrace = new StackTrace(new StackFrame(1, true)).ToString();
                _logger.LogDebug(
                    "Level {QualifiedLevel}/{TotalLevel}: {Message}. StackTrace: {StackTrace}",
                    QualifiedLevel,
                    TotalLevel,
                    StringToProcess,
                    stackTrace
                );
            }
            else
            {
                // 回退到舊的 Debug.WriteLine（向後相容）
                Debug.WriteLine("Time            =" + DateTime.Now.ToString() + Environment.NewLine);
                Debug.WriteLine("StringToProcess =" + StringToProcess + Environment.NewLine);
                StackTrace aStackTraceNextLevel = new StackTrace(new StackFrame(1, true));
                Debug.WriteLine("StackTrace      =" + aStackTraceNextLevel.ToString() + Environment.NewLine);
                Debug.WriteLine("================================================================== " + Environment.NewLine);
            }
        }
    }
    catch (System.Exception e)
    {
        String ErrorString = "ERROR : FullName = " + this.GetType().FullName.ToString() + " , Time = " + DateTime.Now.ToString() + " , Description = " + e.ToString();
        _logger?.LogError(e, ErrorString);
        throw;
    }
}
```

---

## 手動修改步驟

1. **在 Visual Studio 中開啟** `ToolUtility/ToolUtilityClass.cs`
2. **依照批次 1-5 的順序**，逐步修改程式碼
3. **每修改一個批次後**，儲存並編譯驗證
4. **全部完成後**，執行下方的驗收測試

---

## 驗收測試（修改完成後執行）

### 1. 編譯測試
```powershell
# 編譯專案
msbuild ToolUtility\ToolUtility.csproj /p:Configuration=Debug

# 預期結果：0 errors
```

### 2. CA2000 靜態分析
```powershell
# 啟用 Roslyn 分析
dotnet build ToolUtility\ToolUtility.csproj /p:EnableNETAnalyzers=true /p:AnalysisLevel=latest

# 預期結果：無 CA2000 警告
```

### 3. 建立簡單測試（測試舊呼叫仍可運作）
```csharp
// 測試檔案：ToolUtilityTest.cs
using ToolUtilityNameSpace;

class Program
{
    static void Main()
    {
        // 測試 1: 舊的無參數建構式（應仍可運作）
        using (var utility1 = new ToolUtilityClass())
        {
            Console.WriteLine("舊建構式測試通過");
        }

        // 測試 2: 新的帶 ILogger 建構式
        var logger = LoggerFactory.Create(builder => builder.AddConsole())
            .CreateLogger<ToolUtilityClass>();
        
        using (var utility2 = new ToolUtilityClass(logger))
        {
            Console.WriteLine("新建構式測試通過");
        }

        Console.WriteLine("? 所有測試通過！");
    }
}
```

---

## 注意事項

?? **重要**：修改過程中保持小步驟，每批次修改後立即編譯驗證！

?? **備份**：修改前先備份原檔案：
```powershell
Copy-Item "ToolUtility\ToolUtilityClass.cs" "ToolUtility\ToolUtilityClass.cs.backup"
```

?? **Git Commit**：每完成一個批次，建議 commit 一次：
```
git add ToolUtility\ToolUtilityClass.cs
git commit -m "PR-04: 批次 X 完成 - [描述]"
```

---

## 後續步驟（PR-04 完成後）

1. ? 驗證編譯成功
2. ? 執行 CA2000 靜態分析
3. ? 執行檔案鎖定測試
4. ? 更新文件
5. ?? 進行 PR-05（SDK-style csproj + multi-target）

---

**PR-04 狀態**: ?? 等待手動分批修改  
**預估時間**: 1-2 小時  
**風險等級**: ?? Medium（需仔細驗證每個批次）
