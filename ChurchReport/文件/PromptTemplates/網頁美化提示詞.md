提示詞越具體，結果越好：「改哪裡 + 不可動什麼 + 要達成什麼 + 怎麼算完成」。

請直接優化 `ChurchReport/Views/Home/_GeneralGroupGrids.cshtml` 的 UI/UX，目標是更漂亮、吸引人、好操作；僅修改此檔案的 HTML/CSS/JavaScript，保留 DevExtreme DataGrid/Popup 與所有既有資料載入、編輯、儲存、API 呼叫邏輯；請強化視覺一致性（色彩/字級/間距/卡片感）、表格可讀性（表頭、列高、hover、選取狀態）、Popup 操作體驗（分組清楚、按鈕突出、手機可完整捲動），並兼顧 360–430px 手機版可用性；完成後請列出修改項目、UX 改善原因與相容性風險。


請直接修改 `ChurchReport/Views/Home/_GeneralGroupGrids.cshtml`，目標是讓頁面「更漂亮、吸引人、好操作」，但不破壞既有功能。

【技術與範圍限制】
1) 只允許修改此檔案內的 HTML / CSS / JavaScript。
2) 不可修改任何 Controller、API 路由、資料欄位名稱、DataSource 行為。
3) 保留 DevExtreme DataGrid 與 Popup 既有功能（載入、編輯、儲存、通知、圖片載入）。
4) 不可新增外部套件，不可引入新框架。
5) 維持現有中文文案語氣，可微調更清楚但不可改變業務意義。

【UI/UX 改造目標】
A. 視覺一致性
- 統一色彩系統（主色/輔色/邊框/背景）
- 統一標題層級、字級、間距、圓角、陰影
- 區塊改為卡片式設計，提升質感與辨識度

B. DataGrid 可讀性與操作性
- 提升表頭辨識度（背景、字重、間距）
- 列 hover 與選取狀態更明顯
- 強化欄位間距與垂直置中，避免擁擠
- 手機尺寸下維持可用性（避免重要欄位難點擊）

C. Popup 易用性
- 改善區塊分組與視覺層次（標題、間距、分隔）
- 儲存按鈕更突出、固定易點擊
- 表單元件 focus 狀態清楚，減少誤操作
- 手機上可完整閱讀與捲動，不遮住主要按鈕

D. 微互動與回饋
- 保留既有成功/失敗通知邏輯，視覺一致化
- 加入輕量 hover/focus 動畫（不可過度花俏）
- 加強「點擊頭像可編輯」的可發現性

【驗收標準】
1) 桌面與手機都更清楚易用（特別是 360~430px 寬度）。
2) 既有資料載入、更新、儲存流程完全可用。
3) 不新增 JS 錯誤，不影響現有 API 呼叫。
4) 版面更現代、簡潔、教會管理場景可長時間使用不疲勞。

【輸出要求】
- 直接套用修改。
- 最後用條列列出：
  (1) 你改了哪些區塊
  (2) 每項改動提升了什麼 UX
  (3) 有沒有任何相容性風險


  你可以用「5段式提示詞」，成功率最高：
1.	目標：要變漂亮還是變好操作
2.	範圍：指定檔案與區塊（例如 _GeneralGroupGrids.cshtml）
3.	限制：不可改後端 API、不可破壞現有功能、沿用 DevExtreme
4.	驗收標準：你怎麼判斷改好了
5.	輸出格式：要直接改檔、列出變更重點、最後做檢查
---
可直接複製的提示詞（你這個專案適用）

-----------------------------------------------------------------------------------------
提示詞 A：先做整體美化
請針對 `Views/Home/_GeneralGroupGrids.cshtml` 做 UI 美化，目標是「更現代、乾淨、吸引人」。
限制：
- 保留現有功能與資料綁定，不改 Controller/API
- 繼續使用 DevExtreme DataGrid
- 只修改此檔案的 HTML/CSS/前端 JS
需求：
- 統一色彩與字級層級
- 改善區塊標題與卡片感
- 優化表格列高、間距、hover、選取狀態
- Popup 視覺升級（按鈕、間距、資訊分組）
驗收：
- 手機與桌面都可讀
- 不影響原有編輯/儲存流程
請直接實作並說明每一項改動的 UX 理由。
-----------------------------------------------------------------------------------------
提示詞 B：強化好操作（UX）
請優化 `Views/Home/_GeneralGroupGrids.cshtml` 的操作體驗，目標是「更快、更直覺、減少誤操作」。
限制不變：不改後端 API，不刪既有功能。
請實作：
- 更清楚的操作提示（例如點大頭照可編輯）
- 儲存成功/失敗訊息一致化
- 表單欄位焦點與錯誤狀態更明顯
- 重要按鈕固定在易點擊位置（手機友善）
- 降低認知負擔（分段、標籤、圖示一致）
驗收：
- 新手 10 秒內知道怎麼編輯與儲存
- 手機點擊不易誤觸
-----------------------------------------------------------------------------------------
提示詞 C：行動裝置優先
請將 `Views/Home/_GeneralGroupGrids.cshtml` 做 mobile-first 響應式優化。
目標：在 iPhone/Android 上更好看且更好操作。
請實作：
- 小螢幕下縮減不重要欄位、保留關鍵欄位
- Popup 在手機上可完整捲動、不遮擋按鈕
- 按鈕與輸入元件符合手指點擊尺寸
- 字級、間距、對齊在 360px~430px 寬度清晰可讀
驗收：
- 無水平捲動或只在必要處出現
- 重要操作（點名、探訪、儲存）3 步內完成




範圍：
•	僅修改此檔案的 HTML/CSS/JavaScript。 請直接優化 _GeneralGroupGrids.cshtml 的 UI/UX，目標是更漂亮、吸引人、好操作；僅修改此檔案的 HTML/CSS/JavaScript，保留 DevExtreme DataGrid/Popup 與所有既有資料載入、編輯、儲存、API 呼叫邏輯；請強化視覺一致性（色彩/字級/間距/卡片感）、表格可讀性（表頭、列高、hover、選取狀態）、Popup 操作體驗（分組清楚、按鈕突出、手機可完整捲動），並兼顧 360–430px 手機版可用性；完成後請列出修改項目、UX 改善原因與相容性風險。
限制：
•	不可修改 Controller、API 路由、資料欄位名稱、DataSource 行為。
•	保留 DevExtreme DataGrid/Popup 既有載入、編輯、儲存流程。
•	不新增外部套件。


範圍：
•	僅修改此檔案的 HTML/CSS/JavaScript。 請直接優化D:\網頁APP雲端線上版本\DevExpressDevExtreme-21.2.7版本\音訊產品版本\ChurchReport\ChurchReport\Views\Home\MultiGroupView.cshtml 的 UI/UX，目標是更漂亮、吸引人、好操作；僅修改此檔案的 HTML/CSS/JavaScript，保留 DevExtreme DataGrid/Popup 與所有既有資料載入、編輯、儲存、API 呼叫邏輯；請強化視覺一致性（色彩/字級/間距/卡片感）、表格可讀性（表頭、列高、hover、選取狀態）、Popup 操作體驗（分組清楚、按鈕突出、手機可完整捲動），並兼顧 360–430px 手機版可用性；完成後請列出修改項目、UX 改善原因與相容性風險。
限制：
•	不可修改 Controller、API 路由、資料欄位名稱、DataSource 行為。
•	保留 DevExtreme DataGrid/Popup 既有載入、編輯、儲存流程。
•	不新增外部套件。

範圍：
•	僅修改以下檔案的 HTML/CSS/JavaScript。 請直接優化
/Personal/InfomationView
/Personal/MaintainInfomationView
/NewPerson/NewPerson
/FeeManagement/LessonList
/FeeManagement/FeeManagerView
/Dedication/DedicationFeeViewWeb
/Dedication/KeyInDedicationFeeViewWeb
/DedicationAudit/AuditViewWeb
/Home/EquipmentView

的 UI/UX，目標是更漂亮、吸引人、好操作；僅修改此檔案的 HTML/CSS/JavaScript，保留 DevExtreme DataGrid/Popup 與所有既有資料載入、編輯、儲存、API 呼叫邏輯；請強化視覺一致性（色彩/字級/間距/卡片感）、表格可讀性（表頭、列高、hover、選取狀態）、Popup 操作體驗（分組清楚、按鈕突出、手機可完整捲動），並兼顧 360–430px 手機版可用性；完成後請列出修改項目、UX 改善原因與相容性風險。
限制：
•	不可修改 Controller、API 路由、資料欄位名稱、DataSource 行為。
•	保留 DevExtreme DataGrid/Popup 既有載入、編輯、儲存流程。
•	不新增外部套件。


請依序優化以下頁面的 UI/UX 目標是更漂亮、吸引人、好操作；（一次只做一頁，做完就停下等待我回覆「下一頁」）：

1. /Personal/InfomationView
2. /Personal/MaintainInfomationView
3. /NewPerson/NewPerson
4. /FeeManagement/LessonList
5. /FeeManagement/FeeManagerView
6. /Dedication/DedicationFeeViewWeb
7. /Dedication/KeyInDedicationFeeViewWeb
8. /DedicationAudit/AuditViewWeb
9. /Home/EquipmentView

【執行規則】
- 每一輪只處理「當前一頁」對應的 View 檔案。
- 僅修改該檔案的 HTML/CSS/JavaScript。
- 完成後停止，不要自動做下一頁。

【限制】
- 不可修改 Controller、API 路由、資料欄位名稱、DataSource 行為。
- 保留 DevExtreme DataGrid/Popup 既有載入、編輯、儲存流程。
- 不新增外部套件。

【優化重點】
- 視覺一致性（色彩/字級/間距/卡片感）
- 表格可讀性（表頭/列高/hover/選取狀態）
- Popup 易用性（分組/按鈕突出/可捲動）
- 響應式（360–430px）

【每輪輸出格式】
1) 本輪實際修改的檔案路徑
2) 修改區塊清單
3) 每項 UX 改善原因
4) 相容性風險
5) 最後顯示：「已完成第 N 頁，等待『下一頁』指令」


