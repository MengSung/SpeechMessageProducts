# 需求摘要

以來源稽核判定 `ORG-CALL-00047` 是否可安全遷移。輸出必須保留既有 dynamic-list 語意，不得以 static-only
partial migration、shared ToolUtility fallback 或 CRM stored FetchXML 進入 Gateway。此 CCG task 只擁有
`.ccg/tasks/p71-list-member-count-typed-read/` 的紀錄；Trellis child 文件由同名 child 擁有。
