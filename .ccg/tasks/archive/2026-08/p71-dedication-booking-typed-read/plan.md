# P7.1 認獻單強型別讀取能力計畫

1. 寫 registry、client、connector 的 RED contract tests。
2. 新增封閉 wire/DTO/registry/Data8 query/ProductClient/DI，逐組測試轉綠。
3. 執行 release、編碼、scope 與雙模型限時 review，commit/archive task-owned 變更。

外部分析在 45 秒內未產生可用結果，標記為「雙模型未完成」；後續採本機證據與測試驗證。
