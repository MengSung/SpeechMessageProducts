# Merge Requirements

- Source branch: `1.0.0.1.WorkTreeMemberInfo`
- Target branch: `1.0.0.1.MemberInfo`
- Preserve the source worktree and branch after integration.
- Verify source and target cleanliness before merging.
- Use an explicit merge commit with a Traditional Chinese message body.
- Run focused MemberInfo tests, broader regression tests, builds, portable-kit verification, and Git integrity checks.
- Push the target branch and verify local/remote commit equality.

## Review Constraint

The user explicitly waived Gemini and Claude review because provider quotas are exhausted. This task therefore uses local Git inspection, builds, and tests as the review evidence.
