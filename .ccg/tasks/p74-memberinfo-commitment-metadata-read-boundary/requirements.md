# P7.4 MemberInfo 承諾類型 metadata 讀取邊界

此 CCG 記錄與同名 Trellis child 同步。工作只建立預設關閉的 Package03 metadata typed consumer；
false gate 保留 legacy，true gate 僅使用固定 profile/workload/target 的 request-local DTO，
不啟用 CE、流量、P7.5 或 P8。完整需求、設計、驗收與非範圍以
`.trellis/tasks/08-13-p74-memberinfo-commitment-metadata-read-boundary/` 下的 artifacts 為準。
